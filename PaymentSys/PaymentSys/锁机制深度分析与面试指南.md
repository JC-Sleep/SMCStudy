# 支付系统锁机制深度分析与面试指南

> 基于本项目真实代码（`PaymentServiceEnhanced`、`PaymentCallbackServiceEnhanced`、`BaseEntity`）逐行分析

---

## 目录

1. [幂等性是什么？用大白话讲清楚](#幂等性是什么用大白话讲清楚)
2. [系统用了哪些锁？一张图看懂](#一张图看懂)
3. [乐观锁详解 - 代码级别](#乐观锁详解)
4. [Redis分布式锁详解 - 代码级别](#redis分布式锁详解)
5. [为什么不用悲观锁？](#为什么不用悲观锁)
6. [三层防护机制时序图](#三层防护机制时序图)
7. [乐观锁冲突场景图解](#乐观锁冲突场景图解)
8. [有哪些坑？会发生什么问题？](#有哪些坑会发生什么问题)
9. [系统能否正式上线？差距分析](#系统能否正式上线差距分析)
10. [代码修复记录（已修复的Bug）](#代码修复记录)
11. [分布式事务深度对比：Saga vs 2PC/XA vs Seata AT](#分布式事务深度对比)
12. [异步（@Async）是做什么用的？为什么要异步？](#异步async是做什么用的)
13. [性能极限分析：1000万并发会怎样？](#性能极限分析)
14. [限流方案：Guava vs Sentinel（削峰填谷）](#限流方案guava-vs-sentinel)
15. [Nginx 3实例分布式部署：真实QPS上限与全部问题](#nginx-3实例分布式部署分析)
16. [面试必问30题（含答案）](#面试必问30题)

---

## 幂等性是什么？用大白话讲清楚

### 一句话定义

> **同一个操作，不管执行多少次，最终结果都和执行一次一样。**

### 生活中的例子

| 操作 | 幂等？ | 原因 |
|------|--------|------|
| 按电梯"开门"按钮 | ✅ | 门开着，再按还是开着，没变化 |
| 银行转账 100 元 | ❌ 天然不幂等 | 按两次就扣 200 元了！ |
| 设置灯亮度为 50% | ✅ | 不管设多少次，结果都是 50% |
| "给我下一杯咖啡" | ❌ | 说两次就拿两杯 |

---

### 支付系统里的幂等性场景

```
用户点击「支付」按钮
     ↓
前端发送请求 → 网络超时 → 前端看不到响应
     ↓
用户再次点击「支付」（或自动重试）
     ↓
服务器收到两次请求
```

**没有幂等性的结果：**
```
请求1 → 扣款 100 元 ✅
请求2 → 又扣款 100 元 ❌  ← 用户被扣了 200 元！！
```

**有幂等性的结果：**
```
请求1 → 扣款 100 元 ✅  → 返回 txnId=TXN001
请求2 → 检测到重复   ✅  → 直接返回 TXN001（不再扣款）
```

---

### 本项目怎么实现幂等性的？

**核心思路：给每个请求贴一个"唯一标签"（幂等键），处理前先查标签是否存在**

```
前端生成 UUID（幂等Token）：abc-123-xyz
           ↓
第一次请求携带这个 UUID
           ↓
服务端：有没有处理过 abc-123-xyz？
  → 没有 → 正常处理 → 记录"已处理abc-123-xyz"
  → 有了 → 直接返回上次结果，不再处理
```

**本项目三层实现：**

| 层 | 技术手段 | 作用 |
|----|---------|------|
| ① Redis 分布式锁 | `Redisson.tryLock(key)` | 支付发起时，同一幂等键只有一个线程能进 |
| ② DB 幂等键 | `IDEMPOTENCY_KEY` 字段查重 | 双重检查，锁释放后再查一次DB |
| ③ 回调 Redis 去重 | `setIfAbsent(callbackKey)` | 同一回调消息只处理一次 |

---

### 幂等性 vs 去重 vs 乐观锁 的关系（很多人搞混）

```
幂等性（目标）
    ├── 去重（快速判断：这个请求来过吗？）
    │       → Redis setIfAbsent / DB 查询
    └── 乐观锁（并发安全：两个线程同时通过去重怎么办？）
                → @Version + WHERE version=? 只有一个能写成功
```

> 去重是"门卫"，乐观锁是"最后一道闸"，两者配合才能真正做到幂等。

---

## 一张图看懂

```
支付系统锁机制全景
===========================================

┌─────────────────────────────────────────────────┐
│                  支付发起阶段                      │
│   PaymentServiceEnhanced.initiatePayment()       │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │  ① Redis分布式锁（Redisson RLock）        │    │
│  │     Key: payment:lock:{idempotencyKey}  │    │
│  │     等待5秒，持有30秒                     │    │
│  │     → 防止：多实例并发创建重复交易         │    │
│  └─────────────────────────────────────────┘    │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │  ② 数据库幂等键唯一约束（兜底）             │    │
│  │     UNIQUE(IDEMPOTENCY_KEY)             │    │
│  │     → 防止：Redis宕机时的最后防线          │    │
│  └─────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│                  回调处理阶段                      │
│  PaymentCallbackServiceEnhanced.processCallback() │
│                                                  │
│  ┌─────────────────────────────────────────┐    │
│  │  ① Redis去重（setIfAbsent）              │    │
│  │     Key: payment:callback:processed:    │    │
│  │          {gatewayTxId}:{status}         │    │
│  │     TTL: 24小时                         │    │
│  │     → 防止：重复回调瞬时拦截（毫秒级）     │    │
│  └─────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌─────────────────────────────────────────┐    │
│  │  ② 终态检查（业务逻辑层）                  │    │
│  │     SUCCESS/FAILED/REFUNDED → 直接拒绝  │    │
│  │     → 防止：终态被覆盖                    │    │
│  └─────────────────────────────────────────┘    │
│                      ↓                          │
│  ┌─────────────────────────────────────────┐    │
│  │  ③ 乐观锁（MyBatis-Plus @Version）       │    │
│  │     UPDATE WHERE id=? AND version=?     │    │
│  │     → 防止：并发修改导致脏写              │    │
│  └─────────────────────────────────────────┘    │
└─────────────────────────────────────────────────┘

没有使用：悲观锁（SELECT FOR UPDATE）
```

---

## 乐观锁详解

### 代码位置

**第一步：实体类声明 `@Version`（BaseEntity.java）**

```java
@Version
@TableField(value = "VERSION")
private Integer version;
// → 对应数据库 VERSION NUMBER(10) DEFAULT 0 字段
```

**第二步：注册乐观锁拦截器（MyBatisPlusConfig.java）**

```java
interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());
// → 拦截所有 updateById()，自动追加 AND VERSION = ? 条件
```

**第三步：回调处理时使用乐观锁（PaymentCallbackServiceEnhanced.java）**

```java
private boolean updateTransactionStatus(PaymentTransaction transaction,
                                         PaymentCallbackData callbackData) {
    PaymentTransaction update = new PaymentTransaction();
    update.setId(transaction.getId());
    update.setVersion(transaction.getVersion()); // ← 关键：携带当前版本号
    update.setPaymentStatus(callbackData.getPaymentStatus());
    // ...

    int rows = transactionMapper.updateById(update);
    // MyBatis-Plus自动翻译为：
    // UPDATE PAYMENT_TRANSACTION
    //   SET PAYMENT_STATUS=?, VERSION=VERSION+1, UPDATE_TIME=?
    //   WHERE ID=? AND VERSION=?  ← 版本号不匹配则更新0行
    return rows > 0; // 0行=版本冲突，>0行=成功
}
```

### 乐观锁生命周期

```
初始状态：VERSION = 0

交易记录入库 → VERSION = 0
第一次回调更新 → WHERE VERSION=0 → 成功 → VERSION = 1
第二次重复回调 → WHERE VERSION=0 → 失败（0行）→ 抛出OptimisticLockException
```

---

## Redis分布式锁详解

### 代码位置（PaymentServiceEnhanced.java）

```java
private PaymentInitResponse doPaymentWithLock(
        PaymentInitRequest request, PaymentGateway gateway, String idempotencyKey) {

    String lockKey = "payment:lock:" + idempotencyKey;
    RLock lock = redissonClient.getLock(lockKey);  // ① 获取锁对象（还没加锁）

    try {
        // ② 尝试加锁：等待最多5秒，加锁成功后持有30秒
        boolean locked = lock.tryLock(lockWaitTime, lockLeaseTime, TimeUnit.SECONDS);

        if (!locked) {
            throw new PaymentException("系统繁忙，请稍后重试"); // 超时未拿到锁
        }

        // ③ 双重检查（拿到锁后再次查DB，防止前一个请求刚刚创建）
        PaymentTransaction existing = transactionMapper.selectByIdempotencyKey(idempotencyKey);
        if (existing != null) {
            return buildResponseFromTransaction(existing); // 已存在，直接返回
        }

        // ④ 真正创建支付
        return doInitiatePayment(request, gateway, idempotencyKey);

    } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
        throw new PaymentException("系统繁忙，请稍后重试", e);
    } finally {
        // ⑤ 只有当前线程持有时才释放（防止误删他人的锁）
        if (lock.isHeldByCurrentThread()) {
            lock.unlock();
        }
    }
}
```

### Redis去重（回调去重）

```java
private boolean isDuplicateCallbackByRedis(PaymentCallbackData callbackData) {
    String callbackKey = "payment:callback:processed:" +
                         callbackData.getGatewayTransactionId() + ":" +
                         callbackData.getPaymentStatus();

    // 原子操作：SET key value EX 86400 NX
    // 只有key不存在时才设置，成功=首次，失败=重复
    Boolean result = redisTemplate.opsForValue().setIfAbsent(
        callbackKey,
        System.currentTimeMillis(),
        callbackDedupExpire,   // 86400秒 = 24小时
        TimeUnit.SECONDS
    );

    return Boolean.FALSE.equals(result); // false=key已存在=重复回调
}
```

---

## 为什么不用悲观锁？

### 悲观锁是什么（这里指的是）

```sql
-- 悲观锁写法（本项目没用）：
SELECT * FROM PAYMENT_TRANSACTION 
WHERE ID = ? 
FOR UPDATE;  -- ← 行级排它锁，其他线程阻塞
```

### 对比分析

| 维度           | 乐观锁（本项目选用）               | 悲观锁（未选用）                     |
|--------------|-------------------------------|----------------------------------|
| **实现方式**   | `@Version` + WHERE version=?  | `SELECT FOR UPDATE`              |
| **加锁时机**   | 更新时才检查冲突                   | 查询时立即加锁                        |
| **线程阻塞**   | ❌ 不阻塞，直接失败                 | ✅ 阻塞等待锁释放                      |
| **并发性能**   | ✅ 高（无等待）                    | ❌ 低（串行化）                        |
| **适用场景**   | 冲突概率低（一笔订单一般只有1次回调）  | 冲突概率高（账户余额、库存扣减）           |
| **死锁风险**   | ✅ 无死锁                         | ❌ 有死锁风险（多表锁顺序问题）             |
| **DB连接占用** | ✅ 低（不持有锁等待）               | ❌ 高（持锁期间连接不释放）               |
| **Oracle兼容**| ✅ 完全兼容                        | ✅ 兼容，但需要显式 FOR UPDATE          |
| **失败处理**   | 返回冲突异常，上层重试或报错           | 等待超时或死锁回滚                      |

### 支付回调场景为什么适合乐观锁

**核心判断依据：冲突概率低**

```
正常情况：
  一笔交易 → 银行只发一次回调 → 冲突概率 ≈ 0%
  
异常情况：
  网络超时，银行重发 → 极低概率 → 第一次成功后版本已变
  → 第二次触发乐观锁冲突 → 直接拦截，没有问题

使用悲观锁的代价：
  每次处理回调都要 SELECT FOR UPDATE
  → 锁定期间（网关验签 + 业务处理 ≈ 100ms~500ms）
  → 所有其他交易的回调都被阻塞
  → 高并发时吞吐量断崖式下跌
```

---

## 三层防护机制时序图

### 场景1：正常回调（首次）

```
银行网关          CallbackController     Redis             DB(Oracle)
   |                    |                  |                   |
   |-- POST /callback -->|                  |                   |
   |                    |-- setIfAbsent --->|                   |
   |                    |<-- true (首次)  --|                   |
   |                    |                  |                   |
   |                    |-- 签名验证 ------>|                   |
   |                    |-- 查询交易 --------------------------->|
   |                    |<-- 交易(VERSION=0) ------------------|
   |                    |                  |                   |
   |                    |-- 终态检查(PENDING)→ 非终态，继续     |
   |                    |                  |                   |
   |                    |-- UPDATE WHERE VERSION=0 ----------->|
   |                    |<-- 1行更新成功 -----------------------|
   |                    |   (VERSION自动变为1)                  |
   |<-- 200 OK ---------|                  |                   |
```

### 场景2：重复回调（Redis拦截）

```
银行网关          CallbackController     Redis             DB(Oracle)
   |                    |                  |                   |
   |-- POST /callback -->|  (第2次重复)     |                   |
   |  (30秒后重发)        |                  |                   |
   |                    |-- setIfAbsent --->|                   |
   |                    |<-- false(已存在)--|                   |
   |                    |  status=DUPLICATE_REDIS               |
   |                    |  ← 直接拦截，不查DB                   |
   |<-- 200 OK ---------|                  |                   |
   (数据库0次查询！极致性能)
```

### 场景3：Redis宕机时并发回调（乐观锁兜底）

```
线程A（回调1）        线程B（回调2）        DB(Oracle)
   |                    |                   |
   |-- 查询交易 -------->|-- 查询交易 ------->|
   |<-- VERSION=0 ------|<-- VERSION=0 -----|
   |                    |                   |
   |-- UPDATE WHERE     |                   |
   |   VERSION=0 ------>|                   |
   |                    |<-- 1行更新成功 ---|
   |                    |   VERSION 变为 1   |
   |                    |                   |
   |-- UPDATE WHERE     |                   |
   |   VERSION=0 ------>|                   | ← A此时才执行
   |<-- 0行！冲突 -------|                   | ← VERSION已是1，WHERE条件不匹配
   |                    |                   |
   |-- 抛出OptimisticLockException          |
   |   processStatus = RETRY                |
   |   (记录日志，返回200让银行不再重发)     |
```

### 场景4：分布式锁防止重复支付创建

```
用户设备A（请求1）   用户设备B（请求2，网络重发）   Redis     DB(Oracle)
   |                    |                      |           |
   |-- POST /pay ------>|                      |           |
   |                    |-- POST /pay --------->|           |
   |                    |                      |           |
   |-- tryLock -------->|                      |           |
   |  payment:lock:key  |-- tryLock ---------->|           |
   |<-- 成功（获得锁）--|  (等待最多5秒)         |           |
   |                    |                      |           |
   |-- 双重检查 -------------------------------->|           |
   |<-- 不存在 ----------------------------------|           |
   |-- INSERT 交易 ---------------------------------------->|
   |<-- 成功 --------------------------------------------- |
   |-- 释放锁 -------->|                       |           |
   |<-- 200 OK --------|                       |           |
   |                    |<-- 成功（获得锁）----|            |
   |                    |-- 双重检查 ---------------------->|
   |                    |<-- 已存在！返回已有交易 ----------|
   |                    |<-- 200 OK（幂等响应）            |
```

---

## 乐观锁冲突场景图解

### 什么是版本号机制（数据库视角）

```
初始状态：
┌────────┬───────────────┬─────────┐
│   ID   │ PAYMENT_STATUS│ VERSION │
├────────┼───────────────┼─────────┤
│  1001  │    PENDING    │    0    │
└────────┴───────────────┴─────────┘

回调A读取：version=0, status=PENDING
回调B读取：version=0, status=PENDING

回调A执行UPDATE：
UPDATE ... SET STATUS='SUCCESS', VERSION=1 WHERE ID=1001 AND VERSION=0
→ 影响行数：1  ✅ 成功

┌────────┬───────────────┬─────────┐
│   ID   │ PAYMENT_STATUS│ VERSION │
├────────┼───────────────┼─────────┤
│  1001  │    SUCCESS    │    1    │  ← VERSION已更新
└────────┴───────────────┴─────────┘

回调B执行UPDATE（但它拿的还是version=0）：
UPDATE ... SET STATUS='SUCCESS', VERSION=1 WHERE ID=1001 AND VERSION=0
→ 影响行数：0  ❌ 乐观锁冲突！

→ 抛出 OptimisticLockException("版本冲突")
→ 上层捕获，记录 processStatus = RETRY，返回200
→ 支付状态没有被破坏 ✅
```

---

## 有哪些坑？会发生什么问题？

### 🔴 坑1：幂等键策略3（时间戳）导致幂等失效

**代码（PaymentServiceEnhanced.java）：**
```java
// 策略3：订单号 + 金额 + 时间戳（兜底，但不推荐）
String key = request.getOrderReference() + "_" +
             request.getAmount().toString() + "_" +
             System.currentTimeMillis(); // ← 每次调用都不同！
log.warn("使用时间戳幂等键（不推荐），订单号：{}", request.getOrderReference());
```

**问题场景：**
```
用户点击支付 → 请求1（key: ORDER001_100.00_1710000001000）→ 超时
用户重试   → 请求2（key: ORDER001_100.00_1710000001500）→ 成功

结果：两条记录都被插入，同一订单被扣款两次！！
```

**解决方案：**
- 强制要求前端传递 `idempotencyToken`（UUID）
- 或策略2：订单号+支付方式（一般够用）

---

### 🔴 坑2：Redis分布式锁与@Transactional的边界问题

**代码逻辑：**
```java
@Transactional(rollbackFor = Exception.class)  // ← 事务开始
public PaymentInitResponse initiatePayment(...) {
    return doInitiatePaymentWithDistributedLock(...);
}

private PaymentInitResponse doPaymentWithLock(...) {
    RLock lock = redissonClient.getLock(lockKey);
    lock.tryLock(5, 30, SECONDS);

    // ... 业务逻辑 ...

    lock.unlock(); // ← 锁释放了
    // 但事务还没提交！
    // 如果另一个线程在锁释放后、事务提交前进来，
    // 双重检查会查到旧数据（事务隔离）
}
// ← 事务在这里才提交
```

**问题场景（极端情况）：**
```
线程A：获取锁 → 插入记录（未提交）→ 释放锁
线程B：获取锁 → 双重检查（READ COMMITTED，看不到A未提交的数据）→ 再次插入！
              → 触发数据库唯一约束异常（如果有的话，可以作为最终兜底）
```

**解决方案：**
- 在锁内完成整个事务（把`@Transactional`移到`doPaymentWithLock`内部）
- 或数据库加 `UNIQUE INDEX(IDEMPOTENCY_KEY)` 作为终极兜底

---

### 🟡 坑3：乐观锁冲突后没有自动重试

**当前代码：**
```java
if (!updated) {
    log.warn("由于版本冲突更新交易失败");
    callbackLog.setProcessingStatus("RETRY");
    throw new OptimisticLockException("版本冲突"); // ← 抛异常，没有重试！
}
```

**问题：**
- 银行回调收到非200响应 → 银行认为回调失败 → 继续重发
- 重发时Redis去重会拦截（如果用增强版）
- 如果用基础版`PaymentCallbackService`，Redis没有去重，会无限抛异常

**解决方案：**
```java
// 加入重试逻辑（最多3次）
for (int retry = 0; retry < 3; retry++) {
    PaymentTransaction fresh = transactionMapper.selectById(transaction.getId());
    if (isTerminalStatus(fresh.getPaymentStatus())) return; // 已被其他线程处理
    boolean updated = updateTransactionStatus(fresh, callbackData);
    if (updated) return;
    Thread.sleep(50 * (retry + 1)); // 指数退避
}
throw new OptimisticLockException("多次重试后仍版本冲突");
```

---

### 🟡 坑4：Redisson未配置时的降级方案不完善

**代码：**
```java
if (redissonClient != null) {
    return doPaymentWithLock(...); // 分布式锁
} else {
    log.warn("未配置Redis，使用数据库约束保证幂等性（仅适用于单机部署）");
    return doPaymentWithoutLock(...); // 仅查DB
}
```

**问题：**
- 降级方案没有DB唯一约束兜底
- 多实例部署时，未配置Redis会导致并发重复创建交易

---

### 🟡 坑5：回调服务两个版本（基础版/增强版）并存

**问题：**
- `PaymentCallbackService`（基础版）：无Redis去重
- `PaymentCallbackServiceEnhanced`（增强版）：有Redis去重
- 两个Service都注册为Spring Bean，控制器注入哪个？
- 如果注入了基础版，丢失Redis去重这一层防护

---

### 🟡 坑6：`notifyOrderSystem` 是TODO，下游没有真正通知

```java
private void notifyOrderSystem(PaymentTransaction transaction) {
    // TODO: 实现订单系统通知逻辑
    log.info("通知订单系统，订单号：{}", transaction.getOrderReference());
}
```
- 支付成功但订单系统不知道 → 订单永远不会发货
- 生产系统必须实现（通常用消息队列MQ保证可靠性）

---

## 系统能否正式上线？差距分析

### ✅ 已具备的能力

| 能力                  | 状态  | 说明                               |
|---------------------|-------|------------------------------------|
| 基础支付流程          | ✅    | 创建/查询/退款完整                  |
| 多渠道支持            | ✅    | 渣打、支付宝、建行三个渠道           |
| 幂等性（Redis锁）     | ✅    | PaymentServiceEnhanced有完整实现   |
| 乐观锁并发控制        | ✅    | @Version + 拦截器配置正确           |
| 回调签名验证          | ✅    | 每个渠道有独立的verifyCallback      |
| 回调三层防护          | ✅    | Redis去重+终态检查+乐观锁           |
| 异步回调处理          | ✅    | @Async + 独立线程池                 |
| 对账Job              | ✅    | PaymentReconciliationJob            |
| 全局异常处理          | ✅    | GlobalExceptionHandler              |
| 多环境配置            | ✅    | PROD/UAT/DEV                       |

### ❌ 上线前必须解决

| 问题                           | 风险等级 | 解决方案                              |
|------------------------------|--------|-------------------------------------|
| 幂等键策略3（时间戳）           | 🔴 高  | 强制前端传UUID token                 |
| 锁与事务边界（见坑2）           | 🔴 高  | 重构事务范围或加DB唯一约束            |
| `notifyOrderSystem` 未实现     | 🔴 高  | 接入MQ（RocketMQ/Kafka）             |
| 乐观锁无重试逻辑（回调服务）     | 🟡 中  | 加3次指数退避重试                     |
| 两个回调Service并存            | 🟡 中  | 废弃基础版，统一用增强版               |
| 网关实现（渣打）是Mock           | 🔴 高  | 对接真实渣打API，处理网络超时/重试     |
| 没有分布式事务（MQ消息可靠性）   | 🟡 中  | 考虑本地消息表或Seata                  |
| 缺少限流/熔断                  | 🟡 中  | 接入Sentinel或Hystrix                 |
| 没有监控告警                   | 🟡 中  | Prometheus + Grafana + 钉钉告警       |
| 幂等键DB唯一约束缺失            | 🟡 中  | `CREATE UNIQUE INDEX ON PAYMENT_TRANSACTION(IDEMPOTENCY_KEY)` |

---

## 面试必问30题

### 🔵 基础锁概念（5题）

**Q1：乐观锁和悲观锁的区别是什么？**

> **乐观锁**：假设不会冲突，提交时验证（通过version/CAS）。读不加锁，写时检查，冲突则失败重试。
> **悲观锁**：假设一定会冲突，操作前先加锁（`SELECT FOR UPDATE`），别人等待。
> 适用场景：乐观锁适合读多写少/冲突低；悲观锁适合写多/冲突高/强一致。

---

**Q2：乐观锁的CAS是什么原理？**

> Compare And Swap（比较并交换）。包含三个值：内存地址V、期望值A、新值B。
> 当且仅当V的值等于A时，才将V更新为B，否则不修改并返回当前值。
> 本项目实现：`UPDATE ... WHERE id=? AND version=?`（SQL层CAS）

---

**Q3：版本号方案和时间戳方案哪个好？**

> 版本号更好：
> - 时间戳在分布式环境中有时钟漂移问题（NTP同步误差）
> - 时间戳精度不够时可能相同（同一毫秒内两次更新）
> - 版本号单调递增，绝对无冲突歧义

---

**Q4：MySQL的MVCC和乐观锁有什么关系？**

> MVCC是MySQL内部实现的多版本并发控制，对应用层透明，用于支持`READ COMMITTED`和`REPEATABLE READ`隔离级别。
> 乐观锁是应用层手动实现的并发控制（version字段）。
> 两者都是"乐观"思想，但层次不同。MVCC解决读写冲突，应用层乐观锁解决写写冲突。

---

**Q5：什么是ABA问题？乐观锁有没有ABA问题？**

> ABA问题：值从A变为B再变回A，CAS检查时看到A误以为没变化。
> 乐观锁（版本号）没有ABA问题：每次更新版本号递增（0→1→2），即使值回滚，版本号也不会回滚。

---

### 🔵 Redis分布式锁（8题）

**Q6：为什么用Redis做分布式锁而不是Zookeeper？**

> | 维度      | Redis（Redisson）     | Zookeeper              |
> |---------|----------------------|----------------------|
> | 性能      | ✅ 极高（内存操作）    | ❌ 较低（磁盘+网络往返） |
> | 可靠性    | 主从切换可能丢锁       | ✅ CP强一致（Paxos）   |
> | 实现复杂度 | ✅ 简单（SET NX EX） | ❌ 复杂（临时节点+Watch）|
> 支付场景：对性能要求高，短暂重复支付通过幂等键可兜底，选Redis合理。

---

**Q7：Redisson的`tryLock`和普通`lock`有什么区别？**

> - `lock()`：一直阻塞到获取锁（危险！可能永久等待）
> - `tryLock(waitTime, leaseTime, unit)`：最多等待`waitTime`，获得后持有`leaseTime`后自动释放
> 本项目：`tryLock(5, 30, SECONDS)` → 等5秒、持有30秒

---

**Q8：Redisson为什么能防止锁过期后被误删？**

> 普通`SET NX EX`问题：A持锁，业务超时，锁自动过期，B获得锁，A执行完后删除了B的锁。
> Redisson解决：
> 1. 看门狗（Watchdog）：每10秒自动续期，避免业务未完成时锁过期
> 2. 释放前校验：`lock.isHeldByCurrentThread()` → 只有当前线程才能释放
> 本项目代码：`if (lock.isHeldByCurrentThread()) { lock.unlock(); }`

---

**Q9：什么是Redlock（Redis红锁）？本项目有用到吗？**

> Redlock是Redis官方提出的多节点分布式锁方案：向N个（通常5个）独立Redis节点申请锁，超过半数成功才算获锁，防止单点失败。
> 本项目没用Redlock，只用了单节点Redisson锁。
> 对于支付场景，建议使用Redlock，因为Redis主从切换可能导致锁丢失。

---

**Q10：如果Redis宕机了，分布式锁失效，会发生什么？**

> 本项目有降级方案：
> ```java
> if (redissonClient != null) {
>     return doPaymentWithLock(...);
> } else {
>     log.warn("未配置Redis，仅适用于单机");
>     return doPaymentWithoutLock(...); // 仅DB查重
> }
> ```
> 但降级后在多实例部署时，两个实例可能同时通过查重检查，需要DB唯一约束兜底。

---

**Q11：Redis的`setIfAbsent`为什么能保证原子性？**

> `setIfAbsent`对应Redis的`SET key value NX EX seconds`命令。
> Redis是单线程执行命令，所以这是一个原子操作——判断key不存在和设置key是同一个命令，不会有并发问题。
> 注意：不能用先`exists`再`set`的两步操作（非原子，有竞态）。

---

**Q12：为什么回调去重用Redis而支付发起用Redisson？**

> 回调去重：用`setIfAbsent`，只需要"标记已处理"，不需要"排队等待"。一次成功，后续拦截。
> 支付发起：用Redisson`tryLock`，需要"互斥"，同一时间只有一个请求进行支付创建流程，避免并发创建重复交易。
> 本质区别：setIfAbsent是"幂等标记"，Lock是"临界区保护"。

---

**Q13：如果两个回调同时通过了Redis去重（极端情况），如何保证安全？**

> 本项目有三层防护：
> 1. Redis setIfAbsent（大多数情况拦截）
> 2. 终态检查（一个更新成功后状态变为SUCCESS，另一个判断终态直接返回）
> 3. 乐观锁version（即使两个都过了前两关，UPDATE WHERE version=? 只有一个能成功）
> 三层机制确保最终一致性。

---

### 🔵 事务与并发（8题）

**Q14：本项目的事务隔离级别是什么？为什么这样设置？**

> 回调处理设置了`isolation = Isolation.READ_COMMITTED`（提交读）。
> 原因：
> - 避免脏读（读到其他事务未提交的修改）
> - 不用可重复读（REPEATABLE READ）是因为：每次处理回调都是独立的新数据，不需要多次读取一致性
> - Oracle默认就是READ COMMITTED，显式声明是为了代码可读性

---

**Q15：`@Transactional(propagation = Propagation.REQUIRES_NEW)`是什么意思？**

> REQUIRES_NEW：无论外层是否有事务，都新建一个独立事务。外层事务挂起，内层独立提交/回滚。
> 回调处理用这个：
> - 回调是异步的（@Async），本来就没有外层事务
> - 即使有外层，也要独立提交回调日志（即使业务失败，日志也要留下）

---

**Q16：@Async + @Transactional一起用有什么注意事项？**

> 两者配合正常工作，但要注意：
> 1. @Async会让方法在新线程执行，@Transactional绑定的是线程本地（ThreadLocal），所以各自独立事务，没有冲突。
> 2. 主线程的事务不会传播到异步线程（异步线程有自己的事务上下文）。
> 3. @Async方法必须在不同类中调用（同类调用绕过AOP代理，@Async不生效）。

---

**Q17：为什么回调处理不用`synchronized`而用乐观锁？**

> `synchronized`只在单JVM有效，多实例部署时失效。
> 乐观锁在数据库层面生效，多实例都遵守同一个version约束。
> 本项目是分布式系统（多实例部署），`synchronized`完全不够用。

---

**Q18：幂等性、去重、乐观锁三者的关系是什么？**

> - **幂等性**：同一操作执行多次结果相同（业务目标）
> - **去重**：防止同一请求被处理多次（技术手段，快速判断）
> - **乐观锁**：防止并发写入冲突（技术手段，数据一致性）
> 
> 关系：去重和乐观锁都是实现幂等性的技术手段，去重在前（快速）、乐观锁在后（兜底）。

---

**Q19：MyBatis-Plus乐观锁在底层翻译成什么SQL？**

> 原代码：`transactionMapper.updateById(update)`（entity里有@Version字段）
> 
> 翻译后的SQL：
> ```sql
> UPDATE PAYMENT_TRANSACTION
>   SET PAYMENT_STATUS = ?,
>       VERSION = VERSION + 1,   -- 自动加1
>       UPDATE_TIME = ?
>   WHERE ID = ?
>     AND VERSION = ?             -- 带入原版本号
> ```
> 如果版本号不匹配，影响行数为0，MyBatis-Plus返回0，业务判断失败。

---

**Q20：乐观锁失败后，异常会触发事务回滚吗？**

> 会！`@Transactional(rollbackFor = Exception.class)`遇到任何异常都回滚。
> 但乐观锁失败时，通常只是`updateById`返回0，本项目通过返回值判断，不抛异常。
> 如果手动`throw new OptimisticLockException()`，则触发回滚（连已记录的callback_log也回滚）。
> 这是一个需要注意的副作用：finally里的callbackLog插入也会被回滚！

---

**Q21：为什么回调服务要用`REQUIRES_NEW`事务而不是默认的`REQUIRED`？**

> 回调是异步处理，默认没有外层事务，REQUIRED效果等同于REQUIRES_NEW。
> 但显式声明REQUIRES_NEW有明确语义：确保每次回调处理都是独立事务，不受任何外部事务影响。
> 另外，如果未来改成同步处理且在某个大事务内调用，REQUIRES_NEW确保回调日志一定被提交（即使外层事务回滚）。

---

### 🔵 系统设计（9题）

**Q22：支付系统为什么要有幂等性？**

> 支付系统常见场景：
> 1. 网络超时 → 用户重试 → 两次请求到达服务器
> 2. 银行回调 → 服务器处理中断 → 银行再次回调
> 3. MQ消息重发 → 消费者重复消费
> 
> 没有幂等性 → 用户被多次扣款 → 资金损失 → 法律风险
> 幂等性 = 支付系统的基本生命线

---

**Q23：支付状态机设计是什么？**

> 本项目状态流转（单向，不可逆）：
> ```
> INIT → PENDING → SUCCESS → REFUNDED
>      ↘          ↘
>       FAILED    FAILED
>      ↘
>       TIMEOUT
> ```
> 终态：SUCCESS / FAILED / REFUNDED（一旦到达，不可修改）
> 状态机保证：不能从SUCCESS变回PENDING，不能重复扣款

---

**Q24：支付系统的对账是什么？**

> 对账 = 核对本地交易记录与银行记录是否一致
> 本项目`PaymentReconciliationJob`（定时任务）：
> 1. 查询本地PENDING超过N分钟的交易
> 2. 主动调用银行API查询最新状态
> 3. 比较结果：MATCHED（一致）/ MISMATCH（不一致，告警）/ PENDING（银行也不确定）
> 对账是支付系统的最后一道安全网，弥补异步回调的漏洞。

---

**Q25：如果网关回调超时了怎么办？本项目是怎么处理的？**

> 本项目的处理链：
> 1. 银行发回调 → 我们异步处理 → 处理完返回200
> 2. 如果我们宕机，银行一般会重试N次（通常1分钟、5分钟、15分钟...）
> 3. 重试时Redis去重拦截或乐观锁保护
> 4. 如果银行重试都失败：定时对账Job主动查询银行状态补偿

---

**Q26：本项目的异步处理有什么风险？**

> `@Async`使用自定义线程池`callbackExecutor`，风险：
> 1. **队列满**：大量回调时线程池队列满，新回调被拒绝（银行认为失败，重试）
> 2. **线程异常**：异步线程异常不会传播到调用方（回调controller已返回200）
> 3. **事务失效**：需要确保异步线程内有独立事务（本项目已处理）
> 4. **应用重启**：线程池队列中未处理的消息丢失（建议用MQ持久化）

---

**Q27：如何防止退款被重复执行？**

> 当前代码（`PaymentService.refund()`）只检查状态是否为SUCCESS，没有防重复退款！
> 问题：连续调用两次退款API，都可能通过状态检查（两次都是SUCCESS）
> 
> 改进方案：
> ```java
> // 1. 退款也需要幂等键（refundNo唯一约束）
> // 2. 退款时用乐观锁：UPDATE WHERE STATUS='SUCCESS' AND VERSION=?
> // 3. 退款成功后状态改为REFUNDED（终态），下次检查直接拒绝
> ```

---

**Q28：支付系统如何保证资金安全？**

> 多层保障：
> 1. **幂等性**（Redis锁 + DB约束）：防止重复扣款
> 2. **签名验证**：防止伪造回调（`verifyCallback`）
> 3. **状态机**：防止状态非法转换（终态不可逆）
> 4. **乐观锁**：防止并发写入错误
> 5. **对账**：发现并修复异常交易
> 6. **事务**：原子性保证操作完整
> 7. **日志**：`CALLBACK_LOG`全量记录，可审计

---

**Q29：如果让你设计一个支付系统的分布式事务方案，你怎么做？**

> 支付场景适合**最终一致性**方案：
> 
> **Saga模式（推荐）：**
> ```
> 支付成功 → [发MQ消息] → 订单系统消费 → 更新订单状态
>          ↓ 失败            ↓ 消费失败
>       补偿事务           重试 + 死信队列
> ```
> 
> **本地消息表方案：**
> - 支付事务成功时，同库写入一条消息记录（原子）
> - 定时扫描未发送消息，投递到MQ
> - 消费端幂等消费
> 
> **不推荐**：2PC/XA（性能差，DB长期持锁）、Seata AT（侵入性强）

---

**Q30：面试官问："这个系统在高并发下会有什么问题？"怎么回答？**

> **已解决的问题：**
> - 重复支付：Redis分布式锁 + 幂等键
> - 并发回调：三层防护（Redis去重 + 终态检查 + 乐观锁）
> 
> **需要进一步优化的问题：**
> 1. **热点订单**：同一订单被高频查询，加Redis缓存
> 2. **回调洪峰**：银行大量回调时，需要限流（Sentinel）+ 削峰（MQ缓冲）
> 3. **DB连接数**：高并发时Oracle连接池可能耗尽，需要连接池调优（HikariCP）
> 4. **网关超时**：调用渣打API超时，需要熔断降级（Resilience4j）
> 5. **单点Redis**：Redis主节点挂了，分布式锁失效，需要Redis Cluster或Redlock
> 6. **对账效率**：百万级交易对账时，需要分片 + 并行处理

---

## 快速记忆卡

```
支付系统锁机制一句话总结：

支付发起  → Redis分布式锁（Redisson） → 防重复创建
                        ↓
              数据库幂等键（兜底）

回调处理  → Redis去重（setIfAbsent）  → 防重复回调（快）
                        ↓
              终态检查                → 防状态倒退（快）
                        ↓
              乐观锁（@Version）      → 防并发写冲突（最终兜底）

为什么不用悲观锁（SELECT FOR UPDATE）：
  回调冲突概率低 + 悲观锁阻塞影响吞吐 + Redis已做了第一道门
```

---

## 代码修复记录

> 本节记录根据本文分析已实际修复的 Bug，对照"有哪些坑"章节一一落实。

---

### 修复1：退款重复执行（🔴 高危，已修复）

**文件：** `PaymentServiceEnhanced.java` → `refund()` 方法

**问题根因：**
```
原来的代码：
  if (status == SUCCESS) {          ← 两个线程都能通过
      gateway.refund(request)       ← 两个线程都调用了银行退款API！
      update status = REFUNDED
```

**修复方案：引入 `REFUNDING` 中间状态 + 乐观锁原子抢占**

```java
// Step1：原子性地把状态 SUCCESS → REFUNDING（乐观锁保护）
PaymentTransaction claimUpdate = new PaymentTransaction();
claimUpdate.setId(transaction.getId());
claimUpdate.setVersion(transaction.getVersion());  // ← 携带当前版本
claimUpdate.setPaymentStatus(PaymentStatus.REFUNDING.name());

int claimed = transactionMapper.updateById(claimUpdate);
// MyBatis-Plus SQL：UPDATE ... SET STATUS='REFUNDING', VERSION=VERSION+1
//                  WHERE ID=? AND VERSION=?
//                  只有第一个线程：影响行数=1 ✅
//                  第二个线程：VERSION已经变了，影响行数=0 → 直接报错

if (claimed == 0) {
    throw new PaymentException("退款正在处理中，请勿重复提交");
}

// Step2：凭独占权调用银行网关（此刻状态=REFUNDING，其他线程已被拦截）
GatewayRefundResponse response = gateway.refund(request);

// Step3：根据结果更新最终状态
if (response.isSuccess()) {
    → REFUNDED（终态）
} else {
    → REFUND_FAILED（需人工干预）
}
```

**同时新增状态枚举（`PaymentStatus.java`）：**
```java
REFUNDING("退款处理中"),   // 中间状态，防并发
REFUND_FAILED("退款失败"), // 网关异常，需人工核查
```

**修复后的状态机：**
```
INIT → PENDING → SUCCESS → REFUNDING → REFUNDED（终态）
                         ↘ REFUND_FAILED（需人工干预）
```

---

### 修复2：异步线程异常静默丢失（🟡 中危，已修复）

**文件：** `AsyncConfig.java`

**问题根因：**

`@Async` 的 void 方法抛出的异常，Spring 默认**吞掉，没有任何日志**！

```
// 原来的情况（AsyncConfig没有实现 getAsyncUncaughtExceptionHandler）：
@Async
public void processCallback(...) {
    throw new RuntimeException("数据库连接失败"); // ← 消失了！
}
// 日志里什么都看不到，支付状态卡在 PENDING，无人知晓
```

**修复方案：实现 `AsyncUncaughtExceptionHandler`**

```java
@Override
public AsyncUncaughtExceptionHandler getAsyncUncaughtExceptionHandler() {
    return (ex, method, params) -> {
        log.error("【异步任务异常】方法：{}.{}，参数：{}，异常：{}",
            method.getDeclaringClass().getSimpleName(),
            method.getName(),
            Arrays.toString(params),
            ex.getMessage(), ex);
        // TODO 生产环境：触发钉钉/邮件告警
    };
}
```

**同时修复：** 移除文件头乱码 `s噶`（文件编码损坏导致的遗留字符）

---

### 修复3：Controller 注入了基础版 Service（🟡 中危，已修复）

**文件：** `PaymentCallbackController.java`

```java
// 修复前（少了Redis去重这一层）：
@Autowired
private PaymentCallbackService callbackService;  // 基础版，无 Redis 去重

// 修复后：
@Autowired
private PaymentCallbackServiceEnhanced callbackService; // 三层防护全开
```

---

### 修复后的整体防护状态

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 并发退款 | ❌ 可能双重扣款 | ✅ 乐观锁原子抢占，只有一个线程能进 |
| 网关退款异常 | ❌ 状态卡在 REFUNDING 无标记 | ✅ 标记 REFUND_FAILED，运营可查 |
| 异步异常消失 | ❌ 静默，无日志 | ✅ 统一捕获，ERROR 日志 + 可接告警 |
| 回调去重 | ❌ 注入了基础版（无Redis去重）| ✅ 增强版三层防护 |

---

## 分布式事务深度对比

### 为什么支付系统需要分布式事务？

```
支付成功后，需要同时做三件事：
  ① 更新 PAYMENT_TRANSACTION 状态 = SUCCESS   （支付DB）
  ② 通知订单系统：ORDER_001 已付款           （订单DB）
  ③ 给用户发短信通知                          （短信服务）

问题：这三件事在不同服务/数据库里，不是一个本地事务！
      ② 或 ③ 失败了，① 要不要回滚？
```

---

### 方案一：2PC / XA（两阶段提交）

#### 是什么

> 由一个**协调者**分两个阶段统一协调所有**参与者**（数据库）的提交/回滚。

```
阶段1 - Prepare（预提交）：
  协调者 → 参与者A：你能提交吗？→ A：能（锁住资源，写redo log，回复Yes）
  协调者 → 参与者B：你能提交吗？→ B：能（锁住资源，写redo log，回复Yes）

阶段2 - Commit（真正提交）：
  协调者 → 参与者A：提交！→ A：提交完成，释放锁
  协调者 → 参与者B：提交！→ B：提交完成，释放锁
```

#### 最坏的结果时序图

```
时间轴 →→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→

T1  协调者   → 支付DB(A)：Prepare   支付DB回复：Yes，行锁已持有
T2  协调者   → 订单DB(B)：Prepare   订单DB回复：Yes，行锁已持有
T3  协调者   → 支付DB(A)：Commit    
T4  ⚡ 协调者宕机！！！（Commit消息发出去但没收到ACK）
                                     
    支付DB(A)：已提交 ✅  状态=SUCCESS （支付钱已扣）
    订单DB(B)：一直等待 Commit 指令... ← 行锁没有释放！

T5  订单DB(B) 持有行锁等待... 5秒
T6  订单DB(B) 持有行锁等待... 30秒
T7  订单DB(B) 持有行锁等待... 5分钟
    └─ 所有读这个订单的查询全部阻塞！
    └─ 用户刷订单列表：转圈圈...

T8  DBA介入，手动处理，或等协调者恢复
    └─ 协调者恢复后重新发 Commit → 订单DB(B) 提交
    └─ 总计阻塞时间：N 分钟

最坏结果：
  - 支付DB已扣款，但订单状态长时间不更新（用户：我的订单呢？）
  - 订单行锁长期持有，其他用户查询该订单超时
  - 协调者是单点，宕机期间所有分布式事务全部卡死
  - DB连接耗尽：等待锁的连接堆积，引发雪崩
```

**缺点总结：**
- 协调者单点故障 → 所有参与者阻塞
- 参与者持锁等待（阶段1到阶段2之间全程持锁）
- 不适合高并发：100 TPS * 200ms = 20 个并发锁
- 跨数据库（Oracle + MySQL）实现复杂

---

### 方案二：Seata AT 模式

#### 是什么

> Seata 框架的 AT（Auto Transaction）模式：**自动生成回滚SQL**，对业务代码侵入极小（加一个注解）。

```
工作原理：
  Seata 代理数据源，在每次 SQL 执行时：
    - 执行前：保存 before_image（旧数据快照）
    - 执行后：保存 after_image（新数据快照）
    - 生成 undo_log 记录

  需要回滚时：用 undo_log 的 before_image 生成反向 SQL：
    UPDATE ... SET STATUS='PENDING' WHERE ID=? AND STATUS='SUCCESS'
```

#### 最坏的结果时序图

```
时间轴 →→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→

T1  全局事务开始，Seata TC（事务协调器）分配 XID=12345

T2  支付服务：UPDATE STATUS='SUCCESS'
    → Seata 代理保存 undo_log：
      before: {STATUS:'PENDING', VERSION:1}
      after:  {STATUS:'SUCCESS', VERSION:2}

T3  订单服务：UPDATE ORDER_STATUS='PAID'
    → 保存 undo_log

T4  ⚡ 脏读问题出现！！
    ─────────────────────────────────────────
    此时全局事务尚未提交（T2、T3 都只是分支提交）
    另一个查询线程：SELECT * FROM PAYMENT_TRANSACTION WHERE ID=?
    → 读到了 STATUS='SUCCESS'（AT模式默认是本地提交，其他线程可读）
    → 但全局事务还没确认！
    → 如果接下来全局回滚... 这条"已成功"的数据其实是"脏数据"！
    ─────────────────────────────────────────

T5  订单服务：UPDATE ORDER_STATUS 失败（库存不足）

T6  Seata TC 发出全局回滚指令

T7  支付服务收到回滚：
    执行反向SQL：UPDATE STATUS='PENDING' WHERE ID=? AND STATUS='SUCCESS'
    ← 但如果 T4 那个线程已经基于"SUCCESS"做了后续业务呢？
       例如：已经发了"支付成功"短信 ← 短信无法撤回！

T8  ⚡ 另一个更严重的问题：undo_log 被篡改
    ─────────────────────────────────────────
    在 T4 的脏读期间，另一个事务把数据改成了 STATUS='REFUNDED'
    Seata 回滚时：before_image 是 PENDING
    当前值是 REFUNDED（已被改）
    → undo_log 校验失败！→ 抛出异常！
    → 数据不一致！需要人工干预！
    ─────────────────────────────────────────

最坏结果：
  - 脏读：全局事务未提交时，本地事务已可见（READ COMMITTED 隔离级别）
  - 短信/通知已发，但事务回滚，业务语义矛盾
  - undo_log 被并发事务破坏，回滚失败，数据永久不一致
  - 每张业务表都需要一张对应的 UNDO_LOG 表（侵入性强）
  - Seata TC 宕机 → 全局事务阻塞
```

**缺点总结：**
- 脏读问题（AT 模式是 READ COMMITTED，不是全局隔离）
- 需要额外的 UNDO_LOG 表，改造成本高
- 需要部署 Seata Server（TC），多一个基础设施
- 不适合支付核心链路（脏读风险无法接受）

---

### 方案三：Saga 模式（本项目推荐）

#### 是什么

> 把一个长事务拆成多个本地事务，每个本地事务成功后发消息触发下一个；**失败时执行补偿操作（逆向操作）**。没有全局锁，没有协调者单点。

```
正向流程：
  支付成功 → [发MQ消息] → 订单服务消费 → [发MQ消息] → 短信服务消费
  
补偿流程（某步失败）：
  短信发送失败 → 无需补偿（短信不影响资金）
  订单更新失败 → 补偿：把订单状态改回去 → 通知支付服务退款
```

#### 最坏的结果时序图（对比前两个，Saga 的"最坏"轻得多）

```
时间轴 →→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→→

T1  支付服务：UPDATE STATUS='SUCCESS' ← 本地事务立即提交 ✅
    发送 MQ 消息：{type: PAYMENT_SUCCESS, orderId: ORDER_001}

T2  MQ 消息持久化到 Broker ✅

T3  订单服务消费消息：UPDATE ORDER_STATUS='PAID'
    ⚡ 订单服务宕机！！消费失败！

T4  MQ Broker 检测到消费失败 → 自动重试（1s, 5s, 30s, ...）

T5  订单服务重启 → 重新消费消息 → 幂等消费（UPDATE WHERE STATUS!='PAID'）
    → 最终消费成功 ✅

最坏情况1（MQ 消息丢失）：
  T1  支付成功，消息发送失败（MQ Broker 宕机）
  T2  本地没有保存"待发消息"记录
  →   结果：支付成功，但订单永远不知道
  →   解决：本地消息表（把消息写在同一个DB事务里）

本地消息表方案：
  ┌─────────────────────────────────────────────────┐
  │  BEGIN TRANSACTION (同一个本地事务)               │
  │    UPDATE PAYMENT_TRANSACTION SET STATUS='SUCCESS'│
  │    INSERT INTO MESSAGE_OUTBOX                    │
  │      (type, payload, status='PENDING')           │
  │  COMMIT  ← 原子提交，要么都成，要么都不成         │
  └─────────────────────────────────────────────────┘
  定时任务扫描 PENDING 消息 → 投递 MQ → 更新为 SENT

最坏情况2（补偿失败）：
  订单服务补偿重试了 5 次，仍然失败（订单DB宕机超过重试间隔）
  → 进入死信队列（DLQ）→ 人工处理
  → 支付已成功但订单未更新，需要人工运营介入
  → 最终结果：数据最终一致，延迟可能几分钟到几小时
  → 不会出现资金问题，只是订单状态延迟
```

**Saga 的代价（比前两个轻得多）：**
- 最终一致性，不是强一致性（有时间窗口）
- 需要为每个操作写补偿逻辑
- 业务逻辑复杂度增加

---

### 三种方案终极对比

```
对比维度          2PC/XA              Seata AT            Saga（推荐）
─────────────────────────────────────────────────────────────────────
一致性类型         强一致              准强一致             最终一致
持锁时间          ⚠️ 阶段1~2全程持锁   ⚠️ 本地持锁          ✅ 无全局锁
脏读风险          ✅ 无               ⚠️ 有（AT默认）       ✅ 无（已提交）
协调者单点        ❌ 有（TM）          ❌ 有（Seata TC）     ✅ 无
业务代码侵入      ✅ 无侵入            ✅ 仅加注解           ⚠️ 需写补偿
额外基础设施      ❌ XA驱动            ❌ Seata Server       ✅ 仅需MQ
并发性能          ❌ 低（长期持锁）    ⚠️ 中等              ✅ 高
适合场景          银行内部强一致       普通微服务            支付/电商核心链路
最坏后果          协调者宕机→全部阻塞  脏读+undo破坏→不一致  消息延迟→最终一致

本项目建议：Saga + 本地消息表
  理由：支付系统对性能要求高，资金安全靠乐观锁+幂等保证，
       下游（订单/短信）可以接受秒级延迟，Saga最适合。
```

---

### 本项目 Saga 落地方案（最小改动）

```
现状（已有）：
  gateway.refund() 成功后 → notifyOrderSystem()（TODO，未实现）

改造目标（最小成本）：
  notifyOrderSystem() → 发送 MQ 消息
  + 本地消息表保证投递可靠性

Step1：建本地消息表
CREATE TABLE MESSAGE_OUTBOX (
  ID          NUMBER PRIMARY KEY,
  MSG_TYPE    VARCHAR2(50),   -- PAYMENT_SUCCESS / PAYMENT_REFUNDED
  PAYLOAD     CLOB,           -- JSON 消息体
  STATUS      VARCHAR2(20),   -- PENDING / SENT / FAILED
  RETRY_COUNT NUMBER DEFAULT 0,
  CREATE_TIME TIMESTAMP,
  SEND_TIME   TIMESTAMP
);

Step2：支付成功时，同一本地事务写消息
@Transactional
private void onPaymentSuccess(PaymentTransaction tx) {
    // ① 更新支付状态（已有）
    transactionMapper.updateById(...);
    // ② 写本地消息表（同一事务，原子）
    messageOutboxMapper.insert(MessageOutbox.of("PAYMENT_SUCCESS", tx));
}

Step3：定时任务投递
@Scheduled(fixedDelay = 5000)
public void dispatchOutboxMessages() {
    List<MessageOutbox> pending = messageOutboxMapper.selectPending();
    for (MessageOutbox msg : pending) {
        try {
            mqTemplate.send("payment-topic", msg.getPayload());
            msg.setStatus("SENT");
        } catch (Exception e) {
            msg.setRetryCount(msg.getRetryCount() + 1);
            if (msg.getRetryCount() > 5) msg.setStatus("FAILED");
        }
        messageOutboxMapper.updateById(msg);
    }
}

Step4：订单服务幂等消费
@KafkaListener(topics = "payment-topic")
public void onPaymentSuccess(String payload) {
    // 幂等：WHERE ORDER_STATUS != 'PAID'
    int rows = orderMapper.updateStatusIfNotPaid(orderId);
    if (rows == 0) log.info("幂等拦截，订单已处理");
}
```

---

## 异步（@Async）是做什么用的？

### 一句话：银行回调不能让你慢慢处理

支付系统收到银行回调时，需要做很多事：验签名、查数据库、更新状态、通知订单系统……  
这些加起来可能要 **200ms～500ms**。

问题来了：

```
银行服务器发送回调 POST /callback
       ↓
等待你的服务器响应（HTTP 连接挂着）
       ↓
如果你超过 5~30 秒没有回复
       ↓
银行认为回调失败 → 再重发 → 再重发 → 再重发...
（渣打银行标准：重试 5次，间隔1分钟/5分钟/15分钟/30分钟/60分钟）
```

### @Async 的作用：立刻回复，后台慢慢处理

```
银行 → POST /callback
            ↓
       Controller 收到请求
            ↓
       callbackService.processCallback(...)  ← @Async：扔进线程池，立刻返回
            ↓
       立刻 return "SUCCESS"  ← HTTP 连接关闭
            ↓
       银行收到 200，认为成功，不会重发
            ↓ （后台 callback- 线程池继续处理）
       验签名 → 查DB → 乐观锁更新 → 通知订单系统
```

### 代码对照（AsyncConfig.java）

```java
// 这就是那个"后台线程池"
@Bean("callbackExecutor")
public Executor callbackExecutor() {
    executor.setCorePoolSize(10);   // 常驻10个线程，随时待命
    executor.setMaxPoolSize(50);    // 最多扩到50个（应对回调洪峰）
    executor.setQueueCapacity(100); // 最多排队100个（超过则由HTTP线程直接处理）
    executor.setRejectedExecutionHandler(
        new ThreadPoolExecutor.CallerRunsPolicy() // 队列满→HTTP线程兜底，背压保护
    );
    executor.setWaitForTasksToCompleteOnShutdown(true); // 优雅关闭：先把队列处理完
}

// 回调方法用 @Async 标注
@Async("callbackExecutor")
@Transactional(propagation = REQUIRES_NEW)
public void processCallback(String rawBody, ...) {
    // 这段代码在 callback-1, callback-2... 线程上运行，不占用 HTTP 线程
}
```

### 不用 @Async 会发生什么？（对比时序）

```
不用 @Async（同步处理）：
银行         Tomcat HTTP线程(共200个)     DB
  │                    │                  │
  │── 回调1 ───────────→│                  │
  │                    │── 验签(50ms) ─────│
  │                    │── 查DB(100ms) ────│
  │                    │── 更新(50ms) ─────│
  │                    │── 通知订单(200ms) ─│  ← 整个过程 HTTP线程被占用！
  │← 200 OK（400ms后）─│                  │
  │                    │                  │
  银行在等待期间连接挂着，消耗1个HTTP线程

高峰期同时来100个回调：
  → 100个 HTTP 线程被占用（Tomcat默认200个线程）
  → 剩余100个线程处理普通用户请求
  → 如果回调处理慢（DB慢），HTTP 线程耗尽
  → 用户发起支付请求：连接超时！（明明是回调搞的鬼）

用 @Async（异步处理）：
银行         Tomcat HTTP线程     callback-线程池(50个)     DB
  │                 │                    │                  │
  │── 回调1 ────────→│                   │                  │
  │                 │── 扔进线程池 ───────→│                  │
  │← 200 OK(1ms) ───│                   │── 验签/查DB/更新 ─→│
  │  （HTTP线程立刻释放）                 │   （后台慢慢来）  │
  │── 回调2 ────────→│                   │                  │
  │← 200 OK(1ms) ───│  HTTP线程从不阻塞  │                  │
```

### AsyncConfig 当前参数够不够用？

```
当前配置：core=10, max=50, queue=100

支撑能力估算：
  每个回调处理时间 ≈ 200ms
  50个线程 × (1000ms / 200ms) = 250 TPS 回调处理能力
  
  正常情况：每秒支付笔数 ≤ 250 → 足够
  峰值情况：回调积压进入队列（100个缓冲）
  极端情况：CallerRunsPolicy 让HTTP线程兜底，自动降速

如果要支撑 1000 TPS 回调：
  → 把 maxPoolSize 调到 200
  → queueCapacity 调到 1000
  → 或者把回调处理改成 MQ 消费（无上限）
```

---

## 性能极限分析：1000万并发会怎样？

### 先搞清楚"1000万同时支付"的真实含义

```
"1000万同时支付" 实际上是：
  1000万笔请求在 1秒内 同时到达 → QPS = 10,000,000

对比现实：
  双十一高峰：支付宝 峰值 54万笔/秒（54万 QPS）
  微信支付：  高峰   约 100万 QPS
  本系统（单机 Spring Boot）：预估 500~2000 QPS（取决于DB性能）
  
  所以 1000万 QPS = 约是支付宝高峰的 18倍，绝对宕机
```

### 1000万并发打进来会发生什么（时序）

```
T+0s    1000万请求同时到达 Nginx/负载均衡
T+0.1s  Tomcat 线程池（200线程）瞬间全满
         → 请求队列（默认100）瞬间全满
         → 新请求被拒绝（Connection Refused 或 503）
         → 前端：支付失败，请重试
T+0.2s  Redis 连接池（默认50）全满
         → "Cannot get a connection, pool is exhausted"
         → 获取分布式锁失败，抛异常
T+0.3s  Oracle 连接池（HikariCP，默认10）全满
         → "Connection is not available, timeout after 30000ms"
         → 所有DB操作超时
T+0.5s  JVM GC 压力：堆内大量 Request 对象 → Full GC
         → STW（Stop The World）几百ms
         → 所有线程暂停 → 请求全部超时
T+1s    OOM（Out Of Memory）可能触发
         → 应用进程 Kill
         → 服务器宕机

总结：不是"可能宕机"，是"1秒内必定宕机"
```

### 本系统各层的实际瓶颈

```
层次              当前配置           实际瓶颈 QPS     备注
──────────────────────────────────────────────────────────
Tomcat HTTP       maxThreads=200     ~500 QPS         每请求处理约400ms
Redis 分布式锁    连接池=50          ~2000 QPS        内存操作快
Oracle DB         HikariCP=10        ~100~300 QPS     INSERT是瓶颈
callback线程池    max=50             ~250 QPS         异步不占HTTP线程
网关调用(渣打)    HTTP超时5s         ~40 QPS          外部依赖，瓶颈所在

整个系统的最终瓶颈 = min(所有层) ≈ 100~300 QPS（DB限制）
```

---

## 限流方案：Guava vs Sentinel（削峰填谷）

### 为什么要限流？"越多人付钱不是越好吗"

> 这是面试官最喜欢问的问题，答案分三层：

**第一层（直觉层）**：钱越多越好，不应该拒绝用户。  
**第二层（技术层）**：系统有物理上限，超过就全部崩溃，所有人都付不了。  
**第三层（金融安全层）**：支付系统异常洪峰 ≠ 正常流量，可能是 DDoS 攻击、账号盗刷、程序 bug 导致的死循环请求，需要保护。

```
没有限流的结果（1000万并发）：
  1000万人 → 全部失败 → 0笔成功 → 收入 = 0

有限流的结果（系统容量300 QPS）：
  300人/秒 → 全部成功 → 收入 = 300笔/秒
  剩余 999.97万人 → 排队或友好提示"稍后重试"
  
  随着时间推移（30秒内）：
  300 × 30 = 9000笔成功 → 大部分人最终完成支付

限流 = 保护系统稳定 = 保证大多数人能付款成功 = 收入最大化
```

---

### 方案一：Guava RateLimiter（单机，已在项目结构中）

```
适用场景：单机部署 / 简单限流 / 快速接入
限流维度：单个 JVM 进程内的全局限速
```

#### 核心原理（令牌桶算法）

```
令牌桶示意：
  系统以固定速率往桶里放令牌（如 300个/秒）
  每个请求来了先拿令牌，拿到才能处理，没拿到等待或拒绝

  时间轴：
  0s   桶里有 300 令牌
  0s   来了 1000 请求 → 300 个拿到令牌处理 → 700 个等待/拒绝
  1s   桶里又有 300 令牌 → 700 等待中的 300 个继续处理
  2s   桶里再有 300 令牌 → 最后 400 个处理
  
  效果：流量被"削平"到 300 QPS，系统稳定
```

#### 本项目接入代码（可直接用）

```java
// 新建：PaymentRateLimiterConfig.java
@Configuration
public class PaymentRateLimiterConfig {
    
    // 支付发起限流：300笔/秒（根据服务器实际QPS调整）
    @Bean("paymentRateLimiter")
    public RateLimiter paymentRateLimiter() {
        return RateLimiter.create(300.0); // permits per second
    }
    
    // 回调处理限流：500次/秒（回调比支付多，因为有重发）
    @Bean("callbackRateLimiter")
    public RateLimiter callbackRateLimiter() {
        return RateLimiter.create(500.0);
    }
}

// 修改：PaymentController.java
@Autowired
@Qualifier("paymentRateLimiter")
private RateLimiter paymentRateLimiter;

@PostMapping("/initiate")
public ApiResponse<PaymentInitResponse> initiatePayment(@RequestBody PaymentInitRequest request) {
    // tryAcquire(0)：不等待，立刻返回是否拿到令牌
    if (!paymentRateLimiter.tryAcquire()) {
        return ApiResponse.error("系统繁忙，请稍后重试");  // 友好提示
    }
    // 拿到令牌，正常处理
    return ApiResponse.success(paymentService.initiatePayment(request));
}
```

#### Guava 的问题（单机方案天花板）

```
问题1：单机限流，多实例不协调
  实例A 限流 300/s
  实例B 限流 300/s
  → 实际流量 600/s 打到DB，DB还是撑不住

问题2：重启后令牌桶重置
  → 重启瞬间没有历史状态，可能短暂放开大量请求

问题3：无法动态调整
  → 修改限流阈值需要重新部署
  
适合场景：单机部署、TPS < 500、快速上线
```

---

### 方案二：Sentinel（微服务，推荐）

```
适用场景：微服务/分布式部署 / 需要动态调整 / 需要多维度保护
限流维度：集群级别（所有实例合计）、接口级别、用户级别
```

#### 和 Guava 的核心区别

```
Guava（单机）：               Sentinel（集群）：
  实例A: 300/s 令牌桶           所有实例共享 300/s 配额
  实例B: 300/s 令牌桶           实例A 用了 200/s
  实例C: 300/s 令牌桶           实例B 用了 100/s
  ────────────────             → 总量不超过 300/s ✅
  总放行 900/s（超标！）          DB 安全
```

#### 本项目接入代码

```java
// pom.xml 新增依赖
// <dependency>
//   <groupId>com.alibaba.cloud</groupId>
//   <artifactId>spring-cloud-starter-alibaba-sentinel</artifactId>
// </dependency>

// application.yml
// spring.cloud.sentinel.transport.dashboard: localhost:8080

// PaymentController.java
@PostMapping("/initiate")
@SentinelResource(
    value = "payment:initiate",        // 资源名称
    blockHandler = "handleBlock",      // 被限流时调用
    fallback = "handleFallback"        // 业务异常时调用
)
public ApiResponse<PaymentInitResponse> initiatePayment(@RequestBody PaymentInitRequest request) {
    return ApiResponse.success(paymentService.initiatePayment(request));
}

// 限流降级处理
public ApiResponse<PaymentInitResponse> handleBlock(
        PaymentInitRequest request, BlockException ex) {
    log.warn("支付限流触发，订单号：{}", request.getOrderReference());
    return ApiResponse.error("系统繁忙，请稍后重试（限流）");
}

public ApiResponse<PaymentInitResponse> handleFallback(
        PaymentInitRequest request, Throwable ex) {
    log.error("支付降级触发", ex);
    return ApiResponse.error("支付服务暂时不可用，请稍后重试");
}
```

#### Sentinel 控制台配置（无需改代码）

```
Sentinel Dashboard 界面动态配置：

资源名：payment:initiate
规则类型：流量控制
阈值类型：QPS
单机阈值：100（每个实例100，3个实例总计300）

熔断降级（额外保护）：
  资源名：payment:initiate
  熔断策略：异常比例
  阈值：50%（超过50%请求失败，自动熔断10秒）
  ← 防止DB故障时雪崩
```

---

### Guava vs Sentinel 对比一张表

| 维度 | Guava RateLimiter | Sentinel |
|------|------------------|---------|
| **适用部署** | 单机 | 分布式/微服务 |
| **限流精度** | 单实例级别 | 集群级别（精确） |
| **接入成本** | ✅ 极低（1行代码） | 🟡 中（引入Dashboard） |
| **动态调整** | ❌ 改代码重部署 | ✅ 控制台实时调整 |
| **熔断降级** | ❌ 无 | ✅ 内置（异常比例/RT） |
| **热点限流** | ❌ 无 | ✅ 支持（按用户ID/订单号） |
| **监控大盘** | ❌ 无 | ✅ 实时QPS/RT/错误率 |
| **算法** | 令牌桶 | 令牌桶 / 漏桶 / 滑动窗口 |
| **适合场景** | 小型项目快速起步 | 生产级微服务 |
| **本项目建议** | 单机版使用 | 分布式版使用 |

---

### 削峰填谷是什么意思？

```
没有削峰（裸奔）：
请求量  ████████████████████  ← 瞬间洪峰 1000 QPS
服务器  ██████    宕机 !!!

有削峰（令牌桶/MQ缓冲）：
请求量  ████████████████████  ← 洪峰 1000 QPS
限流层  ████                  ← 只放 300 QPS，其余排队/拒绝
服务器  ████ ████ ████ ████   ← 平稳处理，不宕机

填谷 = 低峰期提前处理排队请求（MQ积压慢慢消化）：
凌晨3点  → 正常 10 QPS
服务器   → 100 QPS 处理能力闲置
MQ里     → 还有积压的 10000 笔请求
           → 利用空闲时间全部处理完

最终：每一笔支付请求都会被处理，只是有些延迟
      不会因为瞬间洪峰而丢失请求（除非明确配置超时丢弃）
```

---

### 除了物理扩容，还有哪些其他办法？

> 面试官最喜欢问：**"不加机器，怎么提升系统处理能力？"**

```
纯软件优化层面（不买服务器）：
────────────────────────────────────────────────────────
① 异步化（已实现）
   同步 → 异步 @Async
   效果：回调处理不占 HTTP 线程，吞吐量提升 3~5x

② 数据库连接池调优
   HikariCP: maxPoolSize 从 10 → 50
   效果：并发 DB 操作从 10 → 50，提升 5x
   注意：Oracle 需要对应的 session 授权

③ 批量写入（Batch Insert）
   回调日志：从逐条 INSERT → 批量 INSERT（每 100 条提交一次）
   效果：DB 写 QPS 提升 10~20x

④ Redis 缓存热点数据
   支付状态查询：SELECT → Redis GET（1ms vs 10ms）
   效果：读 QPS 从 500 → 50000

⑤ 读写分离（Oracle Data Guard）
   写操作 → 主库
   查询操作 → 只读副本
   效果：主库压力减半，查询 QPS 翻倍

⑥ 限流 + 削峰（上面讲的）
   效果：保护系统稳定，不是提升吞吐，是保证不宕机

⑦ MQ 解耦（终极方案）
   支付请求 → 写入 MQ（微秒级）→ 消费者慢慢处理
   接入层 QPS 从 300 → 100000（MQ 是内存操作）
   处理层 QPS 还是 300，但不会阻塞前端

物理扩容层面（加机器）：
────────────────────────────────────────────────────────
⑧ 水平扩展（加实例）
   当前：1台 300 QPS
   扩到：3台 900 QPS（需要 Nginx 负载均衡）
   注意：DB 是共享的，不能无限扩，最终瓶颈还是 DB

⑨ 数据库分片（Sharding）
   按订单号哈希，分到 3 个 Oracle 实例
   效果：DB QPS 从 300 → 900
   代价：跨分片查询复杂，事务需要分布式事务

⑩ CDN + 静态资源分离
   支付页面静态资源走 CDN
   只有支付 API 打到服务器
   效果：服务器压力降低 60%~80%
```

#### 架构演进路线（面试标准答案）

```
阶段1（当前）：单机 + Oracle
  瓶颈：300 QPS，靠限流保护

阶段2（短期）：多实例 + Redis Sentinel + 连接池调优
  可支撑：1000 QPS，Sentinel 集群限流

阶段3（中期）：MQ 削峰 + Redis 缓存 + 读写分离
  可支撑：10000 QPS，支付请求异步化

阶段4（长期）：数据库分片 + 多地多活
  可支撑：100000+ QPS，金融级高可用

每个阶段的核心原则：
  先用限流保稳定 → 再做异步提吞吐 → 再做缓存降延迟 → 最后做分片扩容
  不要第一步就想着分片，成本太高，收益不确定
```

---

### 面试连环问答（关于限流和性能）

**Q：为什么要限流？不是越多人付钱越好吗？**
> 没有限流时，1000人同时付钱 → 系统崩溃 → 0人成功 → 收入为0。有限流时，系统稳定在300 QPS，其余人排队，最终大部分人都能付款成功。**限流是为了让更多人能付款成功，不是为了拒绝用户。**

**Q：Guava RateLimiter 和 Sentinel 怎么选？**
> 单机用 Guava（一行代码，零运维），分布式必须用 Sentinel（多实例需要协调总限流额度，Guava 在每个实例上独立限流会导致超标）。

**Q：令牌桶和漏桶的区别？**
> 漏桶：固定速率流出，瞬间洪峰全部排队（对下游绝对平滑）。令牌桶：允许短时间突发（桶里积累的令牌可以瞬间消费），更符合真实业务。支付系统用令牌桶，因为早上开门瞬间的购买洪峰是真实的正常业务。

**Q：限流了用户等着，用户体验很差怎么办？**
> 友好降级：不要返回 503，返回"系统繁忙，预计等待30秒，是否继续？"前端展示排队进度条。配合 MQ 可以做真正的排队（用户拿到排队号，系统异步处理完再通知）。

**Q：物理扩容（加机器）是不是最简单的解决方案？**
> 短期是的，但有边际效应：加 10 台机器，支付服务能力 10x，但 Oracle DB 还是同一个，DB 成了新瓶颈。纯加机器最终会卡在 DB，必须配合读写分离、分片、缓存才能真正线性扩展。

---

## Nginx 3实例分布式部署分析

> 基于 `application.yml` 真实配置，逐层计算实际 QPS，并找出所有分布式问题

---

### 真实配置汇总

```yaml
# application.yml 关键参数
server.tomcat.max-threads: 200          # 每实例 200 个 HTTP 线程
server.tomcat.accept-count:  100        # 队列最多接受 100 个等待连接

spring.datasource.hikari:
  maximum-pool-size: 20                 # 每实例最多 20 个 Oracle 连接
  connection-timeout: 30000ms           # 等 30 秒拿不到连接就报错

spring.redis.lettuce.pool:
  max-active: 50                        # 每实例最多 50 个 Redis 连接

# AsyncConfig.java（callbackExecutor）
callbackExecutor:
  corePoolSize:  10                     # 每实例 10 个常驻回调线程
  maxPoolSize:   50                     # 每实例最多 50 个回调线程
  queueCapacity: 100                    # 每实例最多排队 100 个回调

# SCB 银行 API
scb.timeout.seconds: 120               # 银行 API 最长等 120 秒
```

---

### 精确 QPS 计算（3实例 × 上述配置）

#### 第1层：支付发起 QPS

```
单次支付请求耗时分析（每步串行）：
  ┌───────────────────────────────────────────────────┐
  │ Redis 分布式锁 tryLock()              ≈   5 ms    │
  │ DB SELECT（幂等键查重）               ≈  10 ms    │
  │ DB INSERT（创建交易记录）             ≈  20 ms    │
  │ 渣打银行 API createPayment()          ≈ 2000 ms   │ ← 最大瓶颈！
  │ DB UPDATE（更新网关返回信息）         ≈  20 ms    │
  │ Redis lock.unlock()                   ≈   2 ms    │
  └───────────────────────────────────────────────────┘
  总耗时 ≈ 2057 ms/请求

单实例 Tomcat（200线程）：
  200 线程 ÷ 2.057 s = 97 QPS/实例

3 实例合计：
  97 × 3 = 291 QPS 支付发起

DB 验证（会不会 DB 先撑不住）：
  每笔支付 = 3 次 DB 操作（SELECT + INSERT + UPDATE）
  291 QPS × 3 = 873 次 DB 操作/秒
  60 个 Oracle 连接（3实例×20） × (1000ms/30ms) = 2000 次 DB 操作/秒
  873 < 2000 → DB 不是瓶颈 ✅

结论：支付发起瓶颈 = 渣打银行 API 响应速度（2s/次）
      3实例最多：~ 300 QPS 支付发起
```

#### 第2层：回调处理 QPS（@Async 异步）

```
单次回调处理耗时（callback-线程池执行）：
  ┌───────────────────────────────────────────────────┐
  │ Redis setIfAbsent（去重）             ≈   2 ms    │
  │ DB SELECT（查交易记录）               ≈  10 ms    │
  │ 终态检查（内存操作）                  ≈   0 ms    │
  │ DB UPDATE（乐观锁更新状态）           ≈  20 ms    │
  │ notifyOrderSystem()（目前只打log）    ≈   1 ms    │
  └───────────────────────────────────────────────────┘
  总耗时 ≈ 33 ms/回调

单实例 callbackExecutor（50线程）：
  50 线程 ÷ 0.033 s = 1515 回调/s/实例

3 实例合计（理论值）：
  1515 × 3 = 4545 回调/s

DB 验证（会不会 DB 先撑不住）：
  每次回调 = 2 次 DB 操作（SELECT + UPDATE）
  4545 回调/s × 2 = 9090 次 DB 操作/秒
  60 个 Oracle 连接 × (1000ms/30ms) ≈ 2000 次 DB 操作/秒 容量
  9090 >> 2000 → DB 是瓶颈！

实际回调 QPS（受 DB 限制）：
  2000 DB ops/s ÷ 2 ops/回调 = 1000 回调/s

结论：回调处理 DB 是瓶颈，实际上限 ≈ 1000 回调/s（3实例共享）
```

#### 第3层：查询状态 QPS

```
单次查询耗时：
  DB SELECT（按transactionId查）       ≈  10 ms

DB 可支持：60 连接 × (1000ms/10ms) = 6000 次/s
3实例 Tomcat 限制：600线程 × 100 QPS/线程 = 60000（Tomcat 不是瓶颈）

结论：状态查询上限 ≈ 6000 QPS（Oracle 决定）
      加 Redis 缓存可提升到 50000+ QPS
```

#### 最终 QPS 汇总（3实例 Nginx 部署）

```
┌──────────────────────────────────────────────────────────────────┐
│  操作类型         │ 单实例 QPS │ 3实例合计 │  真正瓶颈              │
├──────────────────────────────────────────────────────────────────┤
│  支付发起         │  ~100 QPS  │  ~300 QPS │  渣打银行 API（2s/次） │
│  回调处理（异步） │  ~330 QPS  │  ~1000 QPS│  Oracle DB（60连接）   │
│  查询状态         │  ~2000 QPS │  ~6000 QPS│  Oracle DB（60连接）   │
│  退款             │  ~50 QPS   │  ~150 QPS │  渣打银行 API + DB     │
├──────────────────────────────────────────────────────────────────┤
│  混合流量综合上限 │            │  ~300 QPS │  银行API（支付是主力） │
└──────────────────────────────────────────────────────────────────┘

没有限流的话：
  正常业务 ≤ 300 QPS → 系统稳定
  峰值突破 300 QPS  → HikariCP 连接池耗尽（30s timeout）→ 用户看到报错
  峰值突破 1000 QPS → Tomcat 队列满（accept-count=100）→ Connection Refused
```

---

### 分布式部署存在的问题（11个，含严重程度）

---

#### 🔴 问题1：对账 Job 在 3 个实例上同时跑（最严重）

**代码位置**：`PaymentReconciliationJob.java` - `@Scheduled` 两个方法  
**现象**：`@Scheduled` 注解在每个 Spring Boot 实例上都会执行，没有分布式协调

```
凌晨 3:00:00  实例A 启动 dailyFullReconciliation()
凌晨 3:00:00  实例B 启动 dailyFullReconciliation()  ← 同时！
凌晨 3:00:00  实例C 启动 dailyFullReconciliation()  ← 同时！

三个实例同时：
  ① 查询昨日所有交易（3次相同的全表扫描，DB负载3×）
  ② 对每笔 PENDING 交易调用渣打银行 API（3倍银行API请求，可能被限流）
  ③ INSERT INTO PAYMENT_RECONCILIATION → 插入3条重复对账记录！
  ④ 对同一笔交易 updateById() → 三个版本冲突，只有一个成功，其他两个静默失败

后果：
  - 对账报告数据×3，财务对账结果错误
  - 银行API调用量3倍，可能触发渣打限流
  - 凌晨3点DB负载3倍，影响业务
```

**修复方案**：
```java
// 方案A：ShedLock（推荐，最简单）
// pom.xml 加：net.javacrumbs.shedlock:shedlock-spring + shedlock-provider-redis-spring
@Scheduled(cron = "0 0 3 * * ?")
@SchedulerLock(name = "dailyFullReconciliation", 
               lockAtMostFor = "2h",   // 最多持锁2小时（防止宕机后永久锁住）
               lockAtLeastFor = "10m") // 至少持锁10分钟（防止抢锁重跑）
public void dailyFullReconciliation() { ... }

// 方案B：Redisson 分布式锁手动控制
@Scheduled(cron = "0 0 3 * * ?")
public void dailyFullReconciliation() {
    RLock lock = redissonClient.getLock("lock:reconciliation:daily");
    if (!lock.tryLock()) return; // 拿不到锁就退出，其他实例在跑
    try { /* 对账逻辑 */ } finally { lock.unlock(); }
}
```

---

#### 🔴 问题2：Snowflake ID 三个实例 workerId 相同 → ID 冲突

**代码位置**：全局使用 `IdUtil.getSnowflakeNextIdStr()`（Hutool 工具类）  
**现象**：Hutool Snowflake 默认 `workerId=0, datacenterId=0`，3个实例相同

```
Snowflake 64位结构：
  [41位时间戳][5位datacenterId][5位workerId][12位序列号]

3个实例都用 workerId=0, datacenterId=0：
  同一毫秒内：
    实例A 生成：time=1000, dc=0, worker=0, seq=1 → ID=XXX001
    实例B 生成：time=1000, dc=0, worker=0, seq=1 → ID=XXX001 ← 相同！
    实例C 生成：time=1000, dc=0, worker=0, seq=1 → ID=XXX001 ← 相同！

后果：
  同一毫秒高并发时，三个实例可能生成相同 ID
  → INSERT 失败（主键冲突）
  → 支付交易无法创建
  → 用户支付失败
```

**修复方案**：
```yaml
# application.yml 三个实例分别配置
# 实例A：
hutool.snowflake.worker-id: 1
hutool.snowflake.datacenter-id: 1
# 实例B：worker-id=2, 实例C：worker-id=3
```
```java
// 启动时从配置读取 workerId，初始化全局 Snowflake
@Configuration
public class SnowflakeConfig {
    @Value("${hutool.snowflake.worker-id:0}")
    private long workerId;
    @Value("${hutool.snowflake.datacenter-id:0}")
    private long datacenterId;

    @Bean
    public Snowflake snowflake() {
        return IdUtil.getSnowflake(workerId, datacenterId);
    }
}
// 所有地方改用注入的 snowflake.nextIdStr() 而不是 IdUtil.getSnowflakeNextIdStr()
```

---

#### 🔴 问题3：Oracle 和 Redis 连接地址默认 localhost → 3实例各连自己

**代码位置**：`application.yml`

```yaml
# 当前配置（危险！）
datasource:
  url: jdbc:oracle:thin:@localhost:1521:ORCL  # ← 每台机器连自己的 localhost

redis:
  host: localhost  # ← 每台机器连自己的 Redis
```

```
如果部署时没有配置环境变量（DB_USERNAME/DB_PASSWORD/REDIS_HOST）：

  实例A → 连自己的 Oracle（192.168.1.1:1521）→ 有数据
  实例B → 连自己的 Oracle（192.168.1.2:1521）→ 没有数据！
  实例C → 连自己的 Oracle（192.168.1.3:1521）→ 没有数据！

用户访问实例A：支付成功，查到记录
用户访问实例B：查不到记录，提示"订单不存在"
```

**修复**：
```bash
# 每个实例启动时必须传入环境变量
java -jar payment.jar \
  -DDB_HOST=192.168.100.10 \       # 共享 Oracle 主机 IP
  -DREDIS_HOST=192.168.100.20 \    # 共享 Redis 主机 IP
  -Dhutool.snowflake.worker-id=1   # 不同实例不同 workerId
```

---

#### 🔴 问题4：对账 Job 只对接渣打，支付宝/建设银行的 PENDING 永不对账

**代码位置**：`PaymentReconciliationJob.java` 第11行

```java
// 写死了只用渣打网关
@Autowired
private StandardCharteredGatewayClient gatewayClient; // ← 只有渣打

// 对账时：
GatewayTransactionStatus gatewayStatus = gatewayClient.queryTransactionStatus(
    transaction.getGatewayTransactionId()
);
// 支付宝/建设银行的 PENDING 交易，永远卡在 PENDING
// 用户付了支付宝，系统永远显示"支付处理中"
```

**修复**：
```java
// 根据 gatewayName 路由到对应渠道查询
PaymentChannel channel = PaymentChannel.fromCode(transaction.getGatewayName());
PaymentGateway gateway = gatewayRouter.getGateway(channel);
GatewayTransactionStatus gatewayStatus = gateway.queryStatus(transaction.getGatewayTransactionId());
```

---

#### 🟡 问题5：银行 API 无熔断器 → 银行慢时整个系统瘫痪

**代码位置**：所有 `gateway.createPayment()` 调用  
**配置**：`scb.timeout.seconds: 120`（2分钟！）

```
渣打银行 API 突然变慢（响应 60 秒）：

T+0s    用户A 发起支付 → 一个 Tomcat 线程占用 60 秒
T+1s    用户B 发起支付 → 又一个 Tomcat 线程占用 60 秒
...
T+120s  200 个 Tomcat 线程全部卡在等待银行 API
T+121s  新请求进来 → Tomcat 队列（100个）满了 → Connection Refused
T+122s  所有用户支付失败，网站无响应

不只影响支付！
  查询状态、退款的请求也进不来（同一个 Tomcat）
  整个服务完全不可用
```

**修复**（接入 Resilience4j 熔断器）：
```java
@CircuitBreaker(name = "scb-gateway", fallbackMethod = "createPaymentFallback")
@TimeLimiter(name = "scb-gateway")  // 超时控制
public GatewayPaymentResponse createPayment(PaymentInitRequest request, String txnId) {
    // 调用渣打银行 API
}

// 熔断时的降级响应
public GatewayPaymentResponse createPaymentFallback(
        PaymentInitRequest request, String txnId, Throwable ex) {
    log.error("渣打银行 API 不可用，熔断触发", ex);
    throw new GatewayException("支付网关暂时不可用，请稍后重试");
}
```

---

#### 🟡 问题6：Redis 单点配置 → Redis 宕机后分布式锁全失效

**代码位置**：`application.yml`

```yaml
redis:
  host: ${REDIS_HOST:localhost}
  port: ${REDIS_PORT:6379}
  # 没有 Sentinel / Cluster 配置
```

```
Redis 宕机后：
  redissonClient 连接失败 → redissonClient = null（或抛异常）
  
  PaymentServiceEnhanced.doInitiatePaymentWithDistributedLock()：
    if (redissonClient != null) → false
    → 走 doPaymentWithoutLock()（无锁模式）
    → Bug-2 问题重现（事务边界问题）
    → 高并发下可能重复创建交易

  PaymentCallbackServiceEnhanced：
    redisTemplate == null → 跳过 Redis 去重
    → 第一层防护失效，直接靠乐观锁兜底
```

**修复**：
```yaml
# Redis Sentinel 模式（高可用）
spring:
  redis:
    sentinel:
      master: mymaster
      nodes:
        - 192.168.100.21:26379
        - 192.168.100.22:26379
        - 192.168.100.23:26379
```

---

#### 🟡 问题7：对账时乐观锁 version 未设置 → 3实例并发对账冲突静默失败

**代码位置**：`PaymentReconciliationJob.reconcileTimeoutTransactions()`

```java
// 对账更新（问题代码）：
transaction.setPreviousStatus(transaction.getPaymentStatus());
transaction.setPaymentStatus(gatewayStatus.getStatus());
transactionMapper.updateById(transaction);
// ↑ transaction 对象里 version 是从 DB 读来的值
// 三个实例同时更新同一笔交易：
//   实例A：UPDATE ... WHERE id=? AND version=0 → 成功，version变1
//   实例B：UPDATE ... WHERE id=? AND version=0 → 0行（version已经是1了）
//   实例C：UPDATE ... WHERE id=? AND version=0 → 0行
// 实例B/C 的更新静默失败，没有任何日志！但对账报告里 matched+1 计数了
// → 对账报告数据不可信
```

**修复**：配合问题1（分布式锁）一起解决，Job 只跑一个实例就不存在并发问题。

---

#### 🟡 问题8：Nginx 没有健康检查 → 实例宕机后仍转发请求

```
假设实例B 宕机：
  Nginx upstream（没有 health_check）：
    → 继续轮询 A、B、C
    → 1/3 的请求打到宕机的 B → Connection Refused
    → 用户看到：约 33% 的支付请求直接失败

Nginx 默认行为：
  等到请求失败后才标记该实例为 down
  这中间的失败请求已经损失了
```

**修复**：
```nginx
upstream payment_backend {
    server 192.168.1.1:8080 max_fails=3 fail_timeout=30s;
    server 192.168.1.2:8080 max_fails=3 fail_timeout=30s;
    server 192.168.1.3:8080 max_fails=3 fail_timeout=30s;
    
    # 或者使用 nginx-plus 的主动健康检查
    # health_check interval=5s fails=3 passes=2;
}
```
或者换成 Spring Cloud + Nacos，服务下线自动摘除。

---

#### 🟡 问题9：日志分散 3 台机器 → 排查问题要登录 3 台服务器

```
用户投诉：支付失败，交易ID = TXN12345

运维流程（没有集中日志）：
  1. 登录实例A → grep TXN12345 logs/payment-system.log → 没找到
  2. 登录实例B → grep TXN12345 logs/payment-system.log → 找到了！
  3. 结合实例A的日志（Redis在A上）→ 再登录A
  总耗时：30分钟 ~ 2小时

有集中日志（ELK/Loki）：
  Kibana 搜索 TXN12345 → 所有实例的日志都在一起
  总耗时：30秒
```

**修复**：接入 ELK（Elasticsearch + Logstash + Kibana）或 Grafana Loki。

---

#### 🟡 问题10：没有任何限流配置 → DB 连接池直接暴露给外部流量

```
当前状态：没有 Guava / Sentinel / 任何限流

促销活动开始瞬间（1000 QPS 到来）：
  T+0s    1000 请求同时到达
  T+0.1s  Tomcat 200线程全满，100个进队列
  T+0.2s  HikariCP 20个连接全满，其余请求等30秒
  T+30s   大量请求 HikariCP timeout → 异常 → 500 错误
  T+30s   用户看到支付失败，但钱可能已经发到银行了（INIT状态）
  T+31s   用户重试 → 加剧问题
```

---

#### 🟢 问题11：日志级别生产环境太详细 → I/O压力 + 安全风险

```yaml
logging:
  level:
    sys.smc.payment: DEBUG  # ← 生产应该是 INFO
```

```
DEBUG 级别会打印：
  - 所有 SQL 语句（含用户ID、订单号、金额）
  - Redis 锁的获取/释放细节
  - 每个 HTTP 请求参数（可能含支付卡信息）

问题：
  1. I/O压力：日志量是 INFO 的 5~10 倍，高QPS时磁盘成为瓶颈
  2. 安全风险：日志文件包含敏感信息，需要严格访问控制
  3. 合规风险：PCI-DSS 要求卡号等数据不能明文出现在日志中
```

---

### 问题严重程度总览

```
🔴 上线即出问题（必须在上线前修复）：
  ① 对账Job 3实例并发执行 → 数据重复，财务报告错误
  ② Snowflake ID 冲突 → INSERT 主键冲突，支付失败
  ③ DB/Redis localhost → 实例各连自己的DB，数据孤岛
  ④ 多渠道对账缺失 → 支付宝/建行 PENDING 永不结束

🟡 上线后遇到压力/故障时暴露（尽快修复）：
  ⑤ 银行API无熔断 → 银行慢时整个服务瘫痪
  ⑥ Redis单点 → Redis宕机后幂等防护下降
  ⑦ 对账乐观锁 → 对账报告数据不可信（配合①修复）
  ⑧ Nginx无健康检查 → 实例宕机后33%请求失败
  ⑨ 日志分散 → 排查问题效率极低
  ⑩ 无限流 → 突发流量打穿连接池

🟢 优化项（有时间就做）：
  ⑪ 日志级别 DEBUG → 改为 INFO，减少I/O，提升安全
```

---

### 修复后的系统能力预估

| 优化项 | 修复前 | 修复后 |
|--------|--------|--------|
| 支付发起 QPS | ~300（受银行API限制） | ~300（加熔断后稳定） |
| 回调处理 QPS | ~1000（受DB限制） | ~3000（加读写分离后） |
| 状态查询 QPS | ~6000（受DB限制） | ~50000（加Redis缓存后） |
| 对账数据准确性 | ❌ 3倍重复 | ✅ 单实例执行 |
| 故障恢复时间 | 30分钟（手动排查） | 5分钟（集中日志+告警） |
| 银行API故障影响 | 整站瘫痪 | 仅支付失败，查询正常 |

---

*文档最后更新：2026-04-16*
*代码修复版本：PaymentServiceEnhanced（退款防重 + 事务锁边界）+ AsyncConfig（异常处理）+ PaymentCallbackController（注入增强版）*

---

## 悲观锁在高并发/分布式中为何性能差？深度图解

> 用户疑问：悲观锁是行锁，不是表锁，不影响其他人的行，为什么说它性能差？

### 先说清楚：行锁 ≠ 没有性能问题

你说得对——`SELECT FOR UPDATE` 锁的是**那一行**，不是整张表。  
但问题不在于"锁了多少行"，而在于：

> **持锁期间，数据库连接被占用，其他线程在应用层排队等待，瓶颈从锁移到了连接池。**

---

### 悲观锁的真实执行流程

```
线程A 执行：SELECT * FROM PAYMENT_TRANSACTION WHERE ID=123 FOR UPDATE
            ↓
  Oracle：给 ID=123 这一行加"行级排他锁"（Row-X Lock）
            ↓
  线程A 持锁，开始执行业务逻辑：
    ├── 验签（调用银行API）  ≈ 50ms ~ 200ms
    ├── 查询订单状态           ≈ 10ms
    ├── 更新状态               ≈ 10ms
    ├── 通知订单系统（异步）   ≈ 5ms
    └── 事务提交               ≈ 5ms
  总计持锁时间 ≈ 80ms ~ 230ms
            ↓
  线程A 提交事务 → 行锁释放
            ↓
  线程B 才能拿到锁，开始执行
```

**问题的本质：在 80ms ~ 230ms 内，线程B 必须持有一个数据库连接等待。**

---

### 连接池耗尽时序图（真实崩溃场景）

假设：数据库连接池 = 50个连接，并发回调 = 200个/秒，每次持锁 = 150ms

```
时间轴 ──────────────────────────────────────────────────────────>

t=0ms    请求1~50 进入，各拿一个DB连接，SELECT FOR UPDATE 锁各自的行
         [连接池：50/50 已用完]

t=10ms   请求51~200 到达，尝试从连接池获取连接
         [连接池：0/50 可用]
         ↓
         请求51~200 全部 [等待连接池]
         （注意：此时他们连"等悲观锁"都还没开始！）

t=150ms  请求1~50 事务提交，释放行锁，释放DB连接
         [连接池：50/50 可用]
         ↓
         请求51~100 抢到连接，进入，SELECT FOR UPDATE
         [连接池：50/50 再次用完]

t=160ms  请求101~200 等待…（已经等了150ms了）

t=300ms  请求51~100 完成，释放

t=450ms  最后一批完成
         ──────────────────────────────
         最慢的请求等待时间 ≈ 300ms ~ 450ms
         而乐观锁方案中，同样200个请求
         ≈ 全部并发执行，最慢 ≈ 150ms 完成
```

**结论：悲观锁把并发请求变成了串行队列，响应时间是悲观锁的 3倍以上。**

---

### 连接池崩溃升级版（银行API变慢时）

```
正常情况：持锁 150ms → 还能撑住
银行API变慢（网络抖动）：持锁 3000ms → 直接崩溃
```

```
时间轴 ──────────────────────────────────────────────────────────>

t=0ms    请求1~50 进入，各拿DB连接，SELECT FOR UPDATE
         然后等待银行API响应（银行正在超时中…）
         [连接池：50/50 用完]

t=100ms  新请求51~500 陆续到达
         [连接池：0可用，全部挂起等待]

t=1000ms 连接池等待超时配置（如 HikariCP 默认30秒）
         请求51~500 开始抛出 "Unable to acquire JDBC Connection"

t=3000ms 请求1~50 银行超时，事务回滚，释放连接
         但此时 请求51~500 全部已超时返回错误！

结果：
  - 用户看到：支付失败（502 / 连接超时）
  - 实际情况：可能部分已扣款成功
  - 对账噩梦：系统显示失败，银行显示成功
```

**这就是为什么说"悲观锁在分布式/高并发中不适用"：它把外部调用（银行API）的延迟直接转化为数据库连接占用时间。**

---

### 分布式场景特有问题：跨节点锁不互通

```
Nginx
  ├── 实例1（Java）── Oracle（行锁 ID=123）← FOR UPDATE
  ├── 实例2（Java）── Oracle（同一行 ID=123）← FOR UPDATE（等待）
  └── 实例3（Java）── Oracle（同一行 ID=123）← FOR UPDATE（等待）
```

```
时序图（3实例同时收到同一笔回调）：

实例1              实例2              实例3         Oracle
  |                  |                  |              |
  |-- FOR UPDATE ---->                               -->|  获得行锁
  |                  |-- FOR UPDATE ----------------->|  ← 等待实例1
  |                  |                  |-- FOR UPDATE->|  ← 等待实例1
  |                  |                  |              |
  |（执行业务逻辑，150ms）               |              |
  |-- COMMIT ---------------------------------->      -->|  行锁释放
  |                  |                  |              |
  |                  |  获得行锁 ---------------------->|
  |                  |（执行业务逻辑…）  |-- 等待实例2 ->|
  |                  |-- COMMIT ---------------------->|
  |                  |                  |  获得行锁 --->|
  |                  |                  |（执行...）    |
  |                  |                  |-- COMMIT ---->|
  
总耗时 = 150ms × 3 = 450ms（串行）
```

**乐观锁方案（同样3实例）：**

```
实例1              实例2              实例3         Oracle
  |                  |                  |              |
  |-- SELECT(V=0) -->|-- SELECT(V=0) -->|-- SELECT --->|  并发查询
  |<-- VERSION=0  ---|<-- VERSION=0  ---|<-- VERSION=0-|
  |                  |                  |              |
  |（各自执行业务逻辑，并发进行）                       |
  |                  |                  |              |
  |-- UPDATE WHERE V=0 ------------------------------------------>|  第一个成功
  |<-- 1行更新 ---------------------------------------------------|
  |                  |                  |              |
  |                  |-- UPDATE WHERE V=0 -------------------->   |  V已=1，失败
  |                  |<-- 0行更新（冲突）----------------------    |
  |                  |（OptimisticLockException，记录日志，return）|
  |                  |                  |              |
  |                  |                  |-- UPDATE WHERE V=0 -->  |  同样失败
  
总耗时 ≈ 150ms（真正并发，只有一个成功，其余立即失败）
```

**乐观锁：并发执行，冲突者立即失败，不等待。**  
**悲观锁：串行排队，所有人都得等第一个人完成。**

---

### 悲观锁会死锁的场景（行锁颗粒度细也没用）

```sql
-- 线程A 执行顺序：先锁 PAYMENT_TRANSACTION，再锁 PAYMENT_RECONCILIATION
BEGIN TRANSACTION;
SELECT * FROM PAYMENT_TRANSACTION WHERE ID=100 FOR UPDATE;        -- ① 锁T行100
SELECT * FROM PAYMENT_RECONCILIATION WHERE TXN_ID=100 FOR UPDATE; -- ③ 等待B释放

-- 线程B 执行顺序：先锁 PAYMENT_RECONCILIATION，再锁 PAYMENT_TRANSACTION
BEGIN TRANSACTION;
SELECT * FROM PAYMENT_RECONCILIATION WHERE TXN_ID=100 FOR UPDATE; -- ② 锁R行100
SELECT * FROM PAYMENT_TRANSACTION WHERE ID=100 FOR UPDATE;         -- ④ 等待A释放
```

```
时序图：

线程A              线程B
  |                  |
  |-- 锁T.100 ------>|  ✅ A拿到T.100
  |                  |-- 锁R.100 -->  ✅ B拿到R.100
  |                  |
  |-- 锁R.100 -----> |  ❌ 等待B释放R.100
  |                  |-- 锁T.100 --> ❌ 等待A释放T.100
  |                  |
  💀 死锁！Oracle检测到，回滚其中一个事务
```

**行锁颗粒度再细，只要涉及多张表/多行，都有死锁风险。乐观锁没有这个问题。**

---

### 悲观锁什么时候应该用？

| 场景 | 是否应用悲观锁 | 原因 |
|------|-------------|------|
| 账户余额扣减（高冲突） | ✅ 可以 | 冲突概率100%，乐观锁会反复重试更慢 |
| 库存秒杀（极高冲突） | ✅ 可以 | 但更好的是 Redis原子操作 |
| 支付状态更新（低冲突） | ❌ 不建议 | 一笔交易一般只有1次回调 |
| 对账Job（单线程执行） | ❌ 不需要 | ShedLock已保证单实例，无并发 |
| 读取报表/查询 | ❌ 绝对不用 | 读操作根本不需要锁 |

**核心原则：**
> - **冲突概率 > 80%** → 悲观锁（或 Redis 原子操作）
> - **冲突概率 < 20%** → 乐观锁
> - **跨服务/分布式** → Redis 分布式锁（完全绕过数据库锁）

---

### 市面上的主流观点

> **不是"永远不用悲观锁"，而是"在支付/回调这个特定场景不用"。**

| 公司/场景 | 选择 |
|-----------|------|
| 阿里支付宝回调处理 | Redis去重 + 乐观锁（和本项目一样） |
| 银行账户余额扣减 | 悲观锁 FOR UPDATE（冲突率极高） |
| 电商库存扣减（高并发） | Redis DECR 原子操作（完全绕开DB锁） |
| 订单状态流转 | 乐观锁（状态机保证单向，冲突率低） |
| 对账数据更新 | ShedLock 分布式调度锁（Job场景） |

---

## 路边销售场景：二次支付 + 银行指令队列

> **真实业务场景**：路边销售给客户点击支付，页面显示"失败"（实际已成功），
> 经理说"宁滥勿缺，再点一次，多了退款"——这合理吗？

---

### 什么是"银行指令队列"（技术名词）

这个技术在支付行业有几个专有名词：

| 名词 | 解释 |
|------|------|
| **Payment Instruction** | 支付指令，商户系统发给银行的扣款请求 |
| **Payment Queue / Instruction Queue** | 银行内部的支付指令队列，收到即入队，异步处理 |
| **Pre-authorization（预授权）** | 先冻结资金，确认后再划扣（信用卡常见） |
| **Authorization & Capture（授权+捕获）** | 两段式支付：先授权，再捕获资金 |
| **Deferred Settlement（延迟清算）** | 银行在业务时间结束后批量清算（T+0/T+1） |
| **Acquirer（收单机构）** | 替商户向银行发起扣款请求的中间机构 |

**你说的"insert进去就行，银行会自动扣钱"** 正是 **Payment Instruction** 模型：

```
商户系统                    银行收单系统（Acquirer）         持卡人账户
    |                              |                           |
    |-- Payment Instruction ------>|                           |
    |   (金额、卡号、商户号)        |  入队 ✅                  |
    |<-- txnRef=TXN20260416001 ----|  (立即返回参考号)          |
    |   (返回很快，毫秒级)          |                           |
    |                              |  异步处理中…               |
    |                              |-- 授权请求 --------------->|
    |                              |<-- 授权成功（资金冻结）----|
    |                              |                           |
    |（几秒到几分钟后）             |                           |
    |<-- 回调：PAYMENT_SUCCESS -----|                           |
    |   或查询API查状态             |  清算划款 --------------->|
    |                              |                           |
```

---

### "宁滥勿缺，多了退款"这个方案合理吗？

**业务角度：可以理解，但有巨大风险。**

#### 场景时序图：同一客户，两次点击，两个不同Token

```
销售员手机        商户系统              银行指令队列          客户账户
     |                |                      |                   |
     |-- 点击支付1 -->|                      |                   |
     |                |-- Instruction #A --->|  入队 ✅           |
     |                |<-- txnRef=TXN-A -----|                   |
     |                |                      |                   |
  （网络超时，页面转圈）|                      |  处理中…           |
     |                |                      |-- 扣款100元 ------>|
     |                |                      |<-- 成功 ----------|
  （30秒后）          |<-- 回调SUCCESS -------|                   |
     |                |  (但销售员没看见)     |                   |
     |                |                      |                   |
     |-- 点击支付2 -->|  (新Token，全新请求)  |                   |
     |  (新Token!)    |-- Instruction #B --->|  入队 ✅           |
     |                |<-- txnRef=TXN-B -----|                   |
     |                |                      |-- 扣款100元 ------>|
     |<-- 支付成功! --|<-- 回调SUCCESS -------|  (又扣一次！)      |
     
最终结果：
  - 客户被扣款 200元（两笔 Instruction 都成功）
  - 商户系统认为"成功了一次"（TXN-B）
  - 需要人工对账发现 TXN-A 也成功，再退款 100元
```

#### 为什么新Token = 无法被幂等拦截

```
幂等Key = TokenA → TXN-A
幂等Key = TokenB → TXN-B  ← 系统认为这是全新的支付，正常处理
```

**新Token意味着：从幂等性系统的角度，这是两笔完全不同的支付。**

---

### 银行有这条记录但钱还没到账户，是真的扣了吗？

这取决于支付模式：

```
模式1：实时授权（信用卡/借记卡刷卡）
  Instruction 入队 → 立即授权（资金冻结）→ 延迟清算（T+0 or T+1）
  
  状态：资金已冻结（客户不能再用这笔钱），但未到商户账户
  风险：清算失败率 < 0.01%，可以认为"授权成功=扣款成功"

模式2：网关支付（微信/支付宝类似）
  Instruction 入队 → 异步处理 → SUCCESS回调后才算成功
  
  状态：入队不等于成功，必须等回调

模式3：批量清算（Transfer/汇款类）
  Instruction 入队 → 每天批量结算（T+1）
  
  状态：入队只是"预约"，清算日才真正扣款
```

**本系统（渣打银行对接）：**

```
1. 发起支付 → 渣打返回 transactionId（入队成功）
              ↓ 此时钱未扣
2. 渣打异步处理 → 回调 /api/payment/callback
              ↓ 收到SUCCESS回调 = 钱已扣
3. 系统更新状态为 SUCCESS
              ↓ 现在才算真正完成
```

**所以"有记录就当扣钱"的说法在渣打这个模式下是错的：必须等到SUCCESS回调。**

---

### 正确的"路边销售"解决方案

**方案一：前端轮询（推荐，不改后端）**

```
销售员点击支付
    ↓
前端发起请求，Token = T001
    ↓
请求超时（但后端可能成功了）
    ↓
前端不要生成新Token！
而是：每隔2秒查一次 /api/payment/status?token=T001
    ↓
如果30秒内查到SUCCESS → 显示成功（避免重复支付）
如果30秒后仍PENDING  → 提示"处理中，请稍后查看"
如果明确失败（FAILED）→ 才允许重试（生成新Token）
```

```
时序图：

销售员手机        商户系统              银行
     |                |                  |
     |-- 支付(T001) ->|-- Instruction --->|
     |   超时         |<-- txnRef=TXN-A --|
     |                |                  |  处理中…
     |-- 查询(T001) ->|-- SELECT -------->|
     |<-- PENDING ----|                  |
     |（2秒后）        |                  |
     |-- 查询(T001) ->|<-- 回调SUCCESS ---|
     |<-- SUCCESS! ---|                  |
     
✅ 只扣一次钱，客户满意，财务清晰
```

**方案二：后端主动查询（加 queryPaymentStatus 接口）**

```java
// 用户点击"再试一次"时，先查原Token状态
PaymentStatus status = paymentService.queryByToken(originalToken);
if (status == SUCCESS) {
    // 直接显示成功，不发起新支付
    return "支付已成功，请查收";
}
if (status == PENDING) {
    return "处理中，请稍等";
}
// 只有明确失败，才允许用新Token重试
```

---

### 经理方案的真实风险评估

| 风险 | 概率 | 危害 |
|------|------|------|
| 双重扣款 | 每次网络超时都可能发生（路边网络差=高概率） | 客户投诉，退款成本，信任损失 |
| 退款操作失败 | 银行退款API也可能失败 | 钱退不回来，纠纷 |
| 对账混乱 | 必然发生（两条记录，一条需核销） | 财务对账效率下降 |
| 监管合规风险 | 渣打等银行对重复支付有监控 | 商户可能被标记为高风险 |
| 客户体验 | 每次失败后需等待退款 | 路边销售场景客户等不了 |

**结论：经理的方案在技术上"能工作"，但对客户和财务都不友好。正确做法是查询优先，失败才重试。**

---

### 面试问：如果面试官问"路边销售支付超时怎么处理"

**回答模板：**

> 支付超时不等于支付失败，正确做法是"幂等重试而非重新支付"。
> 
> 具体方案：
> 1. 前端保留原Token，发起状态查询轮询（3次，每次间隔3秒）
> 2. 查到SUCCESS → 直接返回成功结果，不重复支付
> 3. 查到PENDING → 显示"处理中"，引导用户等待
> 4. 明确FAILED → 才生成新Token发起新支付
>
> 技术术语：这叫"支付指令（Payment Instruction）+ 异步回调 + 状态幂等查询"。
> 
> "宁滥勿缺"的经理方案，在银行网关记账模型下，会导致双重扣款，
> 即使最终退款，也会产生额外的对账工作量、监管合规风险和客户投诉。

---

## 原Token为什么不能直接"续用"重发银行指令？深度解析

> **用户疑问**：银行入队后超时没收到回调，能不能用原来的 Token 继续申请支付？
> 银行队列不是有几分钟时间，连接不是还在用吗？

### 首先要搞清楚三个完全不同的 ID

```
┌──────────────────────────────────────────────────────────────────┐
│  前端 idempotencyToken（UUID）                                    │
│  = 我们系统的去重键                                                │
│  例子：abc-123-xyz                                                │
│  作用：防止商户系统内部重复建单                                    │
├──────────────────────────────────────────────────────────────────┤
│  我们系统 transactionId                                           │
│  = 我们内部交易流水号                                              │
│  例子：TXN8823761234                                              │
│  作用：关联回调、查询、对账                                        │
├──────────────────────────────────────────────────────────────────┤
│  银行 gatewayTransactionId（merchantReference）                   │
│  = 银行那边的指令编号                                              │
│  例子：SCB20260416001                                             │
│  作用：银行内部队列标识，银行用它来去重和追踪                       │
└──────────────────────────────────────────────────────────────────┘

三者关系：
前端Token → 我们系统建单 → 调银行API → 银行生成指令编号
  abc-xyz  →  TXN882376  →  POST /pay  →  SCB20260416001
```

**"银行入队的连接还有用吗"** 这句话混淆了两件事：
- `银行指令`（SCB20260416001）是银行服务器上的一条记录，银行异步处理它
- `HTTP连接`是我们服务器与银行API服务器之间的网络连接，超时后就断了

两者独立！HTTP连接超时不影响银行指令，银行指令仍在队列里继续处理。

---

### 超时后用原Token重试，我们系统代码里发生了什么

```java
// PaymentServiceEnhanced.java - doInitiatePaymentWithDistributedLock()

// 步骤1：生成幂等键（用原Token，idempotencyKey不变）
String idempotencyKey = generateIdempotencyKey(request); // = abc-xyz

// 步骤2：加Redis锁，进入 transactionTemplate.execute()
PaymentTransaction existing = transactionMapper.selectByIdempotencyKey(idempotencyKey);
if (existing != null) {
    // ← 原Token对应的记录状态是 PENDING（上次已建单）
    // → 直接返回现有记录，完全不碰银行！！
    return buildResponseFromTransaction(existing); // 返回：status=PENDING
}
// ← 代码永远不会执行到这里（因为记录已存在）
return doInitiatePayment(request, gateway, idempotencyKey);
```

```
时序图（原Token重试，我们系统的处理）：

销售员APP           商户系统(我们)        Redis           DB(Oracle)        银行
   |                    |                   |                  |               |
   |-- 第1次支付 ------->|                   |                  |               |
   |  Token=abc-xyz     |-- tryLock ------->|                  |               |
   |                    |<-- 获锁成功 -------|                  |               |
   |                    |-- SELECT abc-xyz ------------------>|               |
   |                    |<-- null（不存在）------------------- |               |
   |                    |-- INSERT TXN882376(PENDING) ------->|               |
   |                    |-- POST createPayment ----------------------------------------->|
   |<-- HTTP超时 --------|   （HTTP连接断了）                  |               |  指令入队✅
   |   （看不到响应）     |<-- Exception（连接超时）            |               |
   |   （Token保留！）    |-- UPDATE TXN882376(FAILED?PENDING?)|               |
   |                    |                   |                  |               |
   |                    |                   |                  |               |  （银行继续处理中…）
   |                    |                   |                  |               |
   |-- 第2次支付 ------->|                   |                  |               |
   |  Token=abc-xyz     |-- tryLock ------->|                  |               |
   |  （同原Token！）    |<-- 获锁成功 -------|                  |               |
   |                    |-- SELECT abc-xyz ------------------>|               |
   |                    |<-- TXN882376(PENDING/FAILED) -------|               |
   |                    |  ← 记录存在！直接返回，不发新指令！   |               |
   |<-- 返回PENDING -----|                   |                  |               |
   |   （你要等回调）    |                   |                  |               |
   |                    |                   |                  |               |  银行SUCCESS回调
   |                    |<-- /callback (TXN882376 SUCCESS) ---|               |
   |                    |-- UPDATE PENDING→SUCCESS ---------->|               |
   |-- 查询状态 -------->|                   |                  |               |
   |<-- SUCCESS --------|                   |                  |               |
```

**结论：原Token重试，我们系统的幂等层拦截了，根本不会发第二条银行指令。**
**这是对的！银行那条指令还在处理，不需要再发一条！**

---

### 那问题到底在哪里？—— 超时后状态的二义性

```
我们系统里 TXN882376 的状态 = PENDING（或 FAILED）

问题：
  PENDING = 银行正在处理（指令在队列）→ 应该等
  PENDING = 我们系统以为还在，但银行其实已经失败 → 应该重试

这两种情况，我们系统看到的是同一个 PENDING 状态，无法区分！
```

看代码里的超时处理（`doInitiatePayment` 第261-268行）：

```java
} catch (Exception e) {
    // HTTP连接超时走这里
    transaction.setPaymentStatus(PaymentStatus.FAILED.name()); // ← 标记为FAILED
    transactionMapper.updateById(transaction);
    throw new PaymentException("支付发起失败：" + e.getMessage(), e);
}
```

**当HTTP调用银行超时，代码把我们的记录标记为 FAILED。**
**但是银行那边，指令可能已经入队成功了！**

```
我们系统：TXN882376 = FAILED（因为HTTP超时）
银行系统：SCB20260416001 = 在队列里处理中（SUCCESS即将回调）

状态不一致！此时银行回调 SUCCESS，会发生什么？

银行 → POST /callback (gatewayTransactionId=SCB20260416001)
我们系统：用 gatewayTransactionId 查我们的记录
→ 如果我们存储了 gatewayTransactionId → 能找到 → 更新为SUCCESS ✅（正确）
→ 如果没存（因为HTTP超时，update还没执行）→ 找不到 → 回调丢失 ❌
```

---

### 为什么不能直接"续用原Token发新指令给银行"

```
假设我们允许：原Token + FAILED状态 → 再调一次银行API

时序图（危险场景）：

我们系统              银行（指令队列）
   |                       |
   |-- 第1次调用 ---------->|  指令A入队 ✅
   |<-- HTTP超时 ----------|
   |  我们标记FAILED         |
   |                       |  指令A处理中…
   |                       |
   |-- 第2次调用（原Token）->|  指令B入队 ✅（银行不知道A是同一笔！）
   |<-- 入队成功 -----------|
   |                       |
   |                       |  指令A SUCCESS → 扣款100元 ✅
   |<-- A回调SUCCESS -------|
   |  我们更新为SUCCESS      |
   |                       |
   |                       |  指令B SUCCESS → 扣款100元 ✅（又扣一次！）
   |<-- B回调SUCCESS -------|
   |  我们发现已是SUCCESS   |
   |  → 触发乐观锁冲突      |
   |  → 记录日志，但钱已扣！|

结果：客户被扣了200元！！
```

**所以，在确认银行指令状态之前，绝对不能发第二条指令。**

---

### 四大银行/渠道的指令队列机制对比

#### 渣打银行（Standard Chartered）

```
支付流程：
  商户 → POST /v1/payment/initiate
      参数：merchantTransactionId（= 我们的 transactionId）
  渣打 → 立即返回：{ transactionId, paymentUrl, status: "PENDING" }
       （入队成功，已拿到 paymentUrl，用户去这个URL填卡号）
  渣打 → 异步处理（用户填完卡号授权后）→ 回调 /callback

指令有效期：paymentUrl 通常有效 15分钟 ~ 1小时（SCB可配置）
超时后：指令状态变为 EXPIRED，银行不会再扣款

幂等性：merchantTransactionId 唯一约束
  → 用同一个 merchantTransactionId 再调一次：SCB返回已有记录的状态
  → 不会创建新指令（银行层面有幂等保护）

超时重试方案：
  1. 先调 GET /v1/payment/status?merchantTxnId=xxx 查银行状态
  2. 状态=PENDING → 指令还有效 → 把原 paymentUrl 重新发给用户点击
  3. 状态=EXPIRED → 指令过期 → 创建新指令（用新的 merchantTransactionId）
  4. 状态=SUCCESS → 银行成功了 → 更新我们系统状态，显示成功

关键点：用原Token可以"重推paymentUrl"，但不能"重发指令"
```

```
渣打超时重试时序：

销售员APP       商户系统      渣打银行          DB
   |               |              |              |
   |-- 支付申请 -->|              |              |
   |               |-- POST /pay ->|              |
   |<-- HTTP超时 --|               | 指令入队✅   |
   |               |-- 标记FAILED ->|              |
   |               |              |              |
   |-- 重试（原Token）-->|         |              |
   |               |  发现DB=FAILED              |
   |               |-- 先查银行状态              |
   |               |-- GET /status?txn=TXN882 -->|
   |               |<-- { status: PENDING }------|
   |               |  （银行还在处理！）           |
   |               |-- 更新DB: FAILED→PENDING --->|
   |               |-- 把原paymentUrl返回给用户   |
   |<-- paymentUrl-|              |              |
   |-- 用户点击URL → 填卡号 → 授权             |
   |               |<-- SUCCESS回调 -------------|
   |<-- 支付成功！ -|              |              |
```

---

#### 汇丰银行（HSBC）

```
支付流程：
  商户 → POST /api/payment/create
      参数：orderId（商户唯一订单号）
  汇丰 → 返回：{ hsbcRef, paymentUrl/QRCode }
  汇丰 → 用户支付后异步回调

指令有效期：QRCode通常5~10分钟，paymentUrl 1小时（HSBC可配）

幂等性：orderId 唯一约束（与SCB类似）
  → 同一 orderId 再次提交：HSBC返回原指令状态（不重复创建）

超时重试方案（与SCB基本相同）：
  1. 先查 GET /api/payment/query?orderId=xxx
  2. 返回 PROCESSING → 重推原链接/QR码
  3. 返回 EXPIRED    → 用新orderId创建新指令（意味着需要新的idempotencyToken）
  4. 返回 SUCCESS    → 更新我们状态

特殊点：
  汇丰 PayMe 业务场景：QR码5分钟过期，路边销售经常遇到
  推荐做法：超时后先查状态，未过期则重新显示原QR码（扫码流程，不是URL）
```

---

#### 中国建设银行（CCB）

```
支付流程（网银/龙支付）：
  商户 → POST /api/pay/create
      参数：out_trade_no（商户订单号，唯一）
  建行 → 返回：{ bankOrderNo, payUrl }
  建行 → 异步回调 or 商户主动查询

指令有效期：默认 2小时，可配置最长 7天（对账日关闭）

幂等性：⚠️ 与SCB/HSBC不同！
  建行：同一 out_trade_no 再次提交 → 直接返回 "订单已存在" 错误！
  不会返回已有状态，只报错
  → 必须先查询状态，不能盲目重试

超时重试方案（需要多一步查询）：
  1. POST 失败/超时 → 先查 POST /api/pay/query { out_trade_no }
  2. 返回 WAIT_PAY → 指令存在，重推付款链接
  3. 返回 SUCCESS  → 直接更新成功
  4. 返回 NOT_EXIST → 建行没收到 → 可安全用同一 out_trade_no 重发
  5. 返回 CLOSED   → 已关闭 → 用新 out_trade_no（新idempotencyToken）

⚠️ 特别注意：建行不允许同一out_trade_no重新发起，
   所以一旦"CLOSED"，就必须用新token，跟路边销售场景密切相关
```

---

#### 支付宝（Alipay）

```
支付流程（APP支付/当面付）：
  商户 → POST alipay.trade.create
      参数：out_trade_no（必须唯一）
  支付宝 → 返回：{ trade_no（支付宝单号）, qr_code/payment_url }
  支付宝 → 用户支付后异步回调（notify_url）

指令有效期：
  当面付QR码：默认2分钟（!!! 很短！）
  APP/H5支付：90分钟
  可通过 time_expire 参数自定义

幂等性：支付宝做得最完善
  同一 out_trade_no 的交易处于 WAIT_BUYER_PAY → 重复调用 → 报错 "交易已存在"
  同一 out_trade_no 的交易已 SUCCESS → 重复调用 → 报错 "交易已关闭"

超时重试方案（支付宝推荐的官方流程）：
  1. 发起失败/超时 → 调 alipay.trade.query { out_trade_no }
  2. 返回 WAIT_BUYER_PAY → 还在等待用户支付 → 重新推QR码
  3. 返回 TRADE_SUCCESS → 更新系统状态为SUCCESS
  4. 返回 TRADE_NOT_EXIST → 支付宝没收到 → 可安全重发（同 out_trade_no）
  5. 返回 TRADE_CLOSED → 已关闭 → 必须用新 out_trade_no

⚠️ 路边销售场景下（当面付）：QR码2分钟就过期！
   客户扫码速度慢 → QR失效 → 不是支付失败！是过期！
   正确处理：调 alipay.trade.close 关掉旧单，再开新单（新out_trade_no/新Token）
```

---

### 四行对比总结表

| 维度 | 渣打SCB | 汇丰HSBC | 建行CCB | 支付宝Alipay |
|------|---------|---------|---------|------------|
| 指令有效期 | 15min~1h | 5min~1h | 2h~7d | 2min(QR)/90min(APP) |
| 同ID重复提交 | 返回已有状态✅ | 返回已有状态✅ | 报错❌ | 报错❌ |
| 超时后查询API | GET /status | GET /query | POST /query | alipay.trade.query |
| 未过期重试 | 重推原URL✅ | 重推原QR✅ | 重推原URL✅ | 重推原QR✅ |
| 已过期重试 | 新merchantTxnId（新Token）| 新orderId（新Token）| 新out_trade_no（新Token）| 先close再新open（新Token）|
| 钱什么时候扣 | 用户填卡号授权后 | 用户支付确认后 | 用户网银确认后 | 用户支付确认后 |
| 回调还是轮询 | 回调为主 | 回调为主 | 回调+主动查询 | 回调为主，轮询兜底 |

---

### 正确的"原Token续用"逻辑（需要新增代码）

当前 `PaymentServiceEnhanced.java` 的 `buildResponseFromTransaction()` 只是返回现有状态，
**没有"先查银行状态再决定怎么做"的逻辑**，这是一个设计缺口。

**应该加的逻辑（伪代码）：**

```java
// 当发现现有记录是 PENDING 或 FAILED 时，不能直接返回，要先查银行
PaymentTransaction existing = transactionMapper.selectByIdempotencyKey(idempotencyKey);
if (existing != null) {
    if ("SUCCESS".equals(existing.getPaymentStatus())) {
        return buildResponseFromTransaction(existing); // 成功了，直接返回
    }
    if ("PENDING".equals(existing.getPaymentStatus()) ||
        "FAILED".equals(existing.getPaymentStatus())) {
        // ★ 关键：先查银行的真实状态
        GatewayQueryResponse bankStatus = gateway.queryStatus(existing.getGatewayTransactionId());
        switch (bankStatus.getStatus()) {
            case "SUCCESS":
                // 银行成功了，更新我们的状态，返回成功
                updateTransactionStatus(existing, "SUCCESS");
                return buildSuccessResponse(existing);
            case "PENDING": // 银行还在处理
            case "WAIT_PAY": // 用户还没付
                // 把原来的paymentUrl/QR重新返回给用户
                return PaymentInitResponse.builder()
                    .transactionId(existing.getTransactionId())
                    .paymentUrl(existing.getPaymentUrl()) // 原URL
                    .status("PENDING")
                    .message("支付处理中，请完成支付")
                    .build();
            case "EXPIRED":
            case "CLOSED":
            case "FAILED":
                // 银行那边失败/过期了，这个token可以重新发起新指令
                // （用新的 gatewayTransactionId，但同一个 idempotencyKey 不行！
                //   因为DB有UNIQUE约束，需要新Token）
                throw new PaymentException("原支付已失效，请重新发起支付");
        }
    }
}
```

---

### 路边销售场景最终正确方案（整合各银行）

```
前端逻辑（销售员APP）：

点击支付
  ↓ 生成 Token=abc-xyz，缓存到本地
  ↓ 发送支付请求（带Token）

超时 / 无响应
  ↓ 不要生成新Token！
  ↓ 每3秒轮询一次 GET /payment/status?token=abc-xyz
      → SUCCESS → 显示成功，清除本地缓存Token
      → PENDING + 有paymentUrl → 重新显示付款码/URL给用户点
      → FAILED/EXPIRED → 提示"支付已失效，请重新发起"→ 生成新Token

"重新发起"（只在明确失败时）：
  ↓ 清除本地旧Token
  ↓ 生成新 Token=def-456
  ↓ 发送新支付请求

后端处理：
  新Token → 查不到现有记录 → 正常创建新指令 → 安全！✅
```

**核心原则：**
> - **超时 ≠ 失败**，先查状态
> - **能重推原链接，就不要创建新指令**
> - **明确失败/过期，才用新Token**
> - **新Token = 新指令 = 新扣款机会，必须慎用**

---

## 支付模式全解：为什么要"先建单再跳转"？能不能直接扣钱？

> **用户疑问**：发起支付指令，银行生成订单并返回URL/QR码这一步，是统一标准吗？
> 不能先让用户填好卡号信息，发起支付时直接扣钱吗？

### 答案：不是统一标准，一共有4种支付模式

支付行业有4种完全不同的收款模式，"建单→跳转URL/QR"只是其中**一种**。

---

### 模式一：托管页面支付（Hosted Payment Page）
> 当前本系统（渣打SCB）采用的模式

```
流程：
  商户系统                银行/支付宝              用户
      |                       |                     |
      |-- 创建支付订单 -------->|                     |
      |<-- paymentUrl/QRcode --|                     |
      |                       |                     |
      |-- 把URL/QR给用户 -------------------------------->|
      |                       |<-- 用户输入卡号/扫码--|
      |                       |-- 银行内部验证扣款   |
      |<-- 回调SUCCESS --------|                     |
```

**用户的卡号/支付密码，直接输入在银行/支付宝的页面，商户系统完全看不到。**

```
为什么这样设计？
├── 安全合规：用户卡号不经过商户，商户不需要通过 PCI DSS 认证
├── 责任隔离：信息泄露责任在银行，不在商户
├── 渣打/汇丰/支付宝 都强烈推荐此模式（中小商户几乎只能选这个）
└── 缺点：用户体验有"跳出app"的感觉，有一步跳转
```

---

### 模式二：直接API支付（Direct / Server-to-Server）
> 用户先填好信息，商户直接发指令扣钱 —— 这就是你问的那种！

```
流程：
  商户APP（用户在这里填卡号）
      |
      | 用户填写：
      |   卡号：4111 1111 1111 1111
      |   有效期：12/28
      |   CVV：123
      |   持卡人名：Chan Tai Man
      |
  商户系统
      |-- POST /charge {cardNumber, cvv, amount} ---->  银行API
      |<-- { status: SUCCESS, txnId: xxx } -----------  银行API
      |
  立即扣款完成！不需要跳转，不需要QR码
```

**这就是"直接扣钱"的模式，完全可以实现！**

```
但是代价极其巨大：

┌─────────────────────────────────────────────────────┐
│  ⚠️  PCI DSS（支付卡行业数据安全标准）               │
│                                                     │
│  只要你的系统"接触到"完整卡号（传输、存储、处理），   │
│  你就必须通过 PCI DSS 认证                          │
│                                                     │
│  PCI DSS 认证代价：                                 │
│  ├── 每年审计费用：$10,000 ~ $100,000               │
│  ├── 服务器必须符合特定安全标准（专用网络隔离）        │
│  ├── 日志不得记录卡号（连DEBUG日志也不行）             │
│  ├── 所有数据必须加密（HSM硬件安全模块）               │
│  └── 违规罚款：每次事故 $5,000 ~ $100,000            │
└─────────────────────────────────────────────────────┘

结论：大型银行（汇丰/渣打）的企业客户 可以申请使用这个模式
     中小商户几乎不可能负担合规成本 → 只能用模式一
```

**各银行/渠道对直接API支付的支持情况：**

| 渠道 | 是否支持 | 条件 |
|------|---------|------|
| 渣打SCB | ✅ 支持 | 必须签署特殊协议 + PCI DSS认证 |
| 汇丰HSBC | ✅ 支持 | 必须申请 "Direct API" 权限 |
| 建行CCB | ✅ 支持（B端企业客户） | 需对公账户 + 专线 |
| 支付宝 | ❌ **不支持** | 支付宝从不允许商户接触用户密码 |
| 微信支付 | ❌ **不支持** | 同上，安全设计原则禁止 |

---

### 模式三：Token化支付 / 绑卡免密（Stored Credential）
> 用户第一次填卡，授权后系统保存Token，之后直接扣不需要重新填

```
流程：
  第一次（用户主动填卡并授权）：
  用户在商户APP填卡号 → 商户把卡号发给银行 → 银行返回 cardToken（不是卡号！）
  商户只保存 cardToken（一串随机字符，无法逆推出卡号）

  之后每次续费/购买：
  商户系统 → POST /charge { cardToken, amount } → 银行 → 立即扣款
  用户不需要再填任何东西！
```

```
时序图（绑卡+自动扣款）：

用户（首次绑卡）    商户系统           银行
      |                 |               |
      |-- 填写卡号 ----->|               |
      |                 |-- tokenize --->|  （卡号换Token）
      |                 |<-- cardToken --|
      |                 |  存DB：cardToken=tok_abc123
      |<-- 绑卡成功 -----|               |
      
后续自动扣款（无感知，用户不参与）：
  商户系统 → POST /charge { cardToken=tok_abc123, amount=100 } → 银行
  银行 → 直接扣款 → 回调SUCCESS
  （用户收到短信通知：已扣款100元）
```

**这就是为什么你信用卡绑定在Apple Pay/支付宝后，买东西不用再填卡号！**

```
使用条件：
  ├── 需要用户明确授权（勾选"保存卡信息"）
  ├── 商户保存的是Token，不是真实卡号（符合PCI DSS）
  ├── 适合：订阅服务、续费场景、路边销售常用
  └── 渣打/汇丰/建行都支持，支付宝称之为"代扣"
```

---

### 模式四：预授权（Pre-authorization / Auth-Capture 两步式）
> 先冻结钱，确认后再划扣 —— 酒店/租车常见

```
流程：
  Step 1 - Authorization（授权/冻结）：
  商户 → 发授权请求（金额可以是预估值）→ 银行冻结对应金额
  银行 → 返回 authCode（授权码），用户账户资金被冻结

  Step 2 - Capture（捕获/划扣）：
  商户确认最终金额后 → 发 Capture 请求（附 authCode）
  银行 → 实际划扣资金

  例子：
  酒店入住 → 预授权冻结 $1000（订金）
  退房时 → 实际消费 $600 → Capture $600 → 解冻剩余 $400
```

```
时序图：

客户           酒店前台       渣打银行
  |                |              |
  |-- 刷卡 ------->|              |
  |                |-- Auth $1000 ->|  冻结$1000，但不转账
  |                |<-- authCode ---|
  |  （住了3天）   |              |
  |                |-- Capture $600 -> 实际划走$600
  |                |               |  解冻剩余$400
```

---

### 四种模式完整对比

```
┌──────────────────┬───────────────────┬───────────────────┬──────────────────┐
│                  │ 模式一             │ 模式二             │ 模式三           │ 
│                  │ 托管页面           │ 直接API            │ Token绑卡         │
├──────────────────┼───────────────────┼───────────────────┼──────────────────┤
│ 用户操作         │ 跳转→填卡→返回    │ 在商户APP填卡      │ 首次绑卡后无感   │
│ 扣款速度         │ 用户操作完才扣     │ API调用即扣        │ 自动，秒级        │
│ 商户接触卡号     │ ❌ 从不           │ ✅ 会接触          │ ❌ 只有Token     │
│ PCI DSS要求      │ 低（SAQ A级）      │ 高（SAQ D级，最严）│ 中等             │
│ 路边销售体验     │ 客户点链接填卡    │ 直接刷卡完成       │ 绑卡后自动扣     │
│ 适合场景         │ 普通电商/通用     │ 大型企业POS机      │ 订阅/续费/代扣   │
│ 失败重试处理     │ 重推URL           │ 直接重调API        │ 重调Token扣款    │
│ 本系统（渣打）   │ ✅ 当前实现        │ ❌ 未实现          │ ❌ 未实现         │
└──────────────────┴───────────────────┴───────────────────┴──────────────────┘
```

---

### 路边销售场景最适合哪种模式？

```
路边销售的特点：
  ├── 时效性强（客户站在那里等）
  ├── 网络可能不稳定（路边4G/5G）
  ├── 客户不想跳转APP（体验差）
  └── 可能是重复客户（之前买过）

最佳方案：模式三（Token绑卡）+ 模式一（首次）

流程：
  第一次购买：
    → 客户扫QR码（模式一）→ 跳转银行页面填卡 → 扣款
    → 同时授权"绑卡" → 系统保存 cardToken

  第二次及以后购买：
    → 销售员点击"扣款"
    → 系统用 cardToken 直接 POST /charge
    → 无需客户操作，秒级扣款
    → 客户手机收到银行扣款通知

  好处：
    → 无跳转，不怕网络超时（超时可重试，不会双扣，Token是幂等的）
    → 客户体验极佳（不用掏手机）
    → 支付成功率高（不依赖客户操作）
```

---

### 回答原问题：发起支付指令时，各渠道标准是否统一？

```
不统一！每个渠道的标准完全不同：

渣打SCB（本系统）：
  必须先建单 → 返回paymentUrl → 用户填卡号
  原因：渣打要求商户走托管页面，除非签署特殊企业协议

汇丰HSBC：
  默认走托管页面（PayMe/HSBC PayGate）
  企业客户可申请直接API（需要PCI DSS）

建行CCB（龙支付）：
  B端企业客户可以用"收款码"（类似Token化）
  一般商户走扫码流程（用户扫商户码）

支付宝Alipay：
  永远不给商户接触用户密码
  最近支持路：
    → 当面付：出示收款码（用户扫商户码，或商户扫用户码）
    → 代扣：用户授权后，商户持协议号定期扣款
    → 预授权：冻结+划扣（线下场景专用）

微信支付：
  同支付宝，安全设计不允许商户接触微信支付密码
```

---

### 本系统目前的限制与扩展路径

```
当前实现（模式一）：
  PaymentGateway.createPayment() → 返回 paymentUrl
  → 用户必须点击跳转 → 适合电商/网页场景

如果要支持路边销售（建议扩展）：

Step 1：向渣打申请"Recurring Payment"（循环扣款）权限
Step 2：第一次收款走现有流程，同时在回调里保存 cardToken
Step 3：扩展 PaymentGateway 接口，增加 chargeByToken() 方法

代码扩展位置（PaymentGateway接口）：
  // 现有
  GatewayPaymentResponse createPayment(request, txnId); // 建单→返回URL
  
  // 新增（路边销售直接扣款）
  GatewayPaymentResponse chargeByToken(cardToken, amount, currency, txnId);
  
  // 新增（查询支付状态，用于超时重试）
  GatewayQueryResponse queryStatus(gatewayTransactionId);
```


