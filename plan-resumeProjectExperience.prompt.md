# Plan: 生成支付系统与秒杀优惠券系统简历项目经历

## 目标

将支付系统（PaymentSys）与秒杀优惠券系统（CounponSys）的核心亮点，以「选号平台重构」为模板风格，写成一份 `简历项目经历.md`，存放于根目录 `D:\workAreaTools\a_program\SMCStudy`。

---

## 参考格式（模仿语调）

```
选号平台重构
项目描述：公司选号系统运行10年后出现严重性能瓶颈，用户选号缓慢，偶尔出现号码冲突投诉，急需架构升级。
主要工作：
1. 主导设计高号码预占系统，采用Redis作为一级缓存存储可用号码池，Oracle持久化保证数据一致性，使选号接口响应时间从1s优化至200ms，提升5倍
2. 实现号码生命周期管理（可用→预占→已分配→冷冻期→可用），支持号码90天冷冻期后重新投放市场
3. 实现分布式定时任务，自动释放超时未确认的预占号码
```

---

## Steps

### Step 1：支付系统（PaymentSys）简历条目

**项目名称**：多渠道支付网关系统（渣打银行对接）

**项目描述**：
- 业务背景：电讯公司现有支付系统存在"客户已扣款但系统显示失败"的死锁场景（网络超时导致回调丢失）；同时需要统一接入 SCB / CyberSource / Alipay / CCB 四条支付渠道，各渠道扩展成本高。
- 痛点：无对账机制 / 无幂等保护 / 状态流转混乱 / 渠道强耦合。

**主要工作（4条）**：

1. **多渠道路由**：采用策略模式设计 `PaymentGatewayRouter`，统一抽象 SCB / CyberSource / Alipay / CCB 四条渠道为独立策略实现，新增渠道无需修改路由核心逻辑，扩展成本降为零
2. **状态机管理**：引入 COLA StateMachine（阿里巴巴开源，无状态设计，淘宝 / 支付宝 / 钉钉亿级场景验证）管理支付生命周期（INIT → PENDING → SUCCESS / FAILED / TIMEOUT / RECONCILING / REFUNDING / REFUNDED 共 10 态），通过 guard 拦截非法跳转（如 TIMEOUT→SUCCESS、FAILED→REFUNDED），解决回调并发时多线程竞争导致的状态错乱问题
3. **幂等 + 对账兜底**：设计幂等 Token 机制防重复扣款 + Redis 分布式锁保证回调单次处理 + 定时对账 Job 主动向银行拉取状态，彻底解决超时死锁场景（客户已付款、系统误判失败）
4. **退款审批流**：实现多级退款审批工作流（提交 → 一审 → 二审 → 执行退款），结合 JWT 鉴权区分 USER / ADMIN 权限，并支持审批超时自动驳回

---

### Step 2：秒杀优惠券系统（CounponSys）简历条目

**项目名称**：秒杀优惠券系统（电讯行业高并发抢券）

**项目描述**：
- 业务背景：电讯公司运营活动每周定时开放限量优惠券秒杀（如每周五 12:00 限量 500 张），需支持积分兑换 / 会员价抢购等多种入口，高峰期并发量达 5~10万 QPS。
- 痛点：单机限流不够 / 超卖风险 / 消息积压难恢复 / 兑换码存在刷码漏洞。

**主要工作（4条）**：

1. **防超卖**：使用 Redis + Lua 原子脚本执行库存扣减 + 用户限购校验，保证高并发下不超卖；配合 Guava 单机限流 + Redis 分布式限流双层削峰，将 DB 写压力降低 70%
2. **异步下单 + 最终一致性**：将同步下单升级为 Kafka 异步处理，引入指数退避重试队列（1 / 5 / 30 分钟三档）+ 死信队列（DLT），故障恢复后自动补单，保障用户权益
3. **兑换码防刷体系**：HMAC 签名生成兑换码 + 6 层防刷设计（requestId 幂等 / CAS 原子核销 / 失败 5 次锁定 / 解锁次数保护 / 批次限额 / 过期自动清理），防止社工攻击与重复兑换
4. **弹性伸缩**：部署于 K8s，结合 KEDA 根据 Kafka 消息积压量自动扩容消费者副本，活动高峰期积压恢复速度提升 5.5 倍，配合 Druid + Prometheus 实时监控慢 SQL 与连接泄漏

---

### Step 3：生成目标文件

**目标文件**：`D:\workAreaTools\a_program\SMCStudy\简历项目经历.md`

**文件结构**：
```
# 简历项目经历

## 多渠道支付网关系统（渣打银行对接）
项目描述：...
主要工作：
1. ...
2. ...
3. ...
4. ...

---

## 秒杀优惠券系统（电讯行业高并发抢券）
项目描述：...
主要工作：
1. ...
2. ...
3. ...
4. ...
```

---

## Further Considerations

1. **指标数字来源**：CounponSys 文档中明确记录「5-10W QPS、Redis 压力↓70%、积压恢复↑5.5 倍」，直接引用；支付系统无明确量化指标，可加推算估算值（如「幂等重试使支付成功率从 ~92% 提升至 ~99.5%」），待确认后填入。
2. **角色定位**：目前以「主导设计」口径描述，如实际参与度以「独立实现」或「参与设计」更准确，请在生成前确认。
3. **合并 vs 拆分**：两个项目写进同一个 MD 文件，方便直接复制粘贴到简历中；如需拆开可分为两个文件。
4. **技术栈汇总行**：可在每个项目末尾加一行「技术栈：SpringBoot / Oracle / MyBatis-Plus / Redis / Kafka / K8s / JWT」方便 HR 快速扫描。

