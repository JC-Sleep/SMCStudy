# 简历项目经历

---

## 多渠道支付网关系统（渣打银行对接）

**项目描述**：公司原有支付系统强耦合单一渠道，扩展新渠道需大量侵入式改造；同时生产环境频繁出现"客户已扣款、系统显示失败"投诉——银行回调因网络超时丢失后系统无任何兜底机制，财务与客户双方陷入死锁僵局、无法退款也无发货凭证。系统亦缺乏渠道故障自愈能力，月结账单日若渠道宕机整条支付链路即告中断，急需从架构层面全面重建。

**主要工作**：
1. 主导设计多渠道支付路由层，以策略模式将 SCB（渣打）/ CyberSource（国际信用卡）/ Alipay / CCB（建行）四条渠道抽象为独立策略实现，统一由 `PaymentGatewayRouter` 分发；同时集成 PCI DSS 合规方案：接入 CyberSource Flex Microform v2，卡号在前端直接加密后传 CyberSource，商户后端从不接触明文卡号，彻底规避 PCI 审计范围

2. 引入 COLA StateMachine（阿里巴巴开源，核心 ~400 行，淘宝 / 支付宝 / 钉钉亿级场景验证）管理支付全生命周期，定义 10 个状态・13 条合法转换（INIT→PENDING→SUCCESS / FAILED / TIMEOUT→RECONCILING→SUCCESS|FAILED；SUCCESS→REFUNDING→REFUNDED / PARTIALLY_REFUNDED）；无状态设计无需持久化机器实例，并发千笔共用单一 Bean；通过 `guard` 拦截非法跳转（TIMEOUT→SUCCESS、FAILED→REFUNDED 等），配合 `@Transactional(REQUIRES_NEW)` + 乐观锁 version 字段保证"校验通过→写 DB"不被竞争，彻底解决回调并发时多线程竞争导致的状态错乱问题

3. 专项治理"客户付款-系统死账"场景：设计三重幂等兜底（幂等 Token 防重复扣款 + Redis 分布式锁保证回调单次处理 + 定时对账 Job 主动向银行拉取未决状态并走 RECONCILING 审计中间态修正）；增加`ORDER_NOTIFIED` 字段建立双防线通知链路（即时回调通知 + `OrderSuccessNotificationJob` 每 5 分钟兜底扫描），确保支付成功后订单系统 100% 感知，死账场景降为零

4. 设计三态熔断器 `GatewayHealthMonitor`（CLOSED / OPEN / HALF_OPEN），基于连续失败次数自动切断故障渠道，半开期以单笔试探决定是否恢复，月结账单日渠道宕机时系统自动切换备用渠道，主链路 TPS 从 < 500 升级至 > 5000；同步实现多级退款审批工作流（提交 → 财务一审 → 经理二审 → 异步执行退款），支持多次部分退款、审批超时自动驳回，结合 JWT 鉴权区分 USER / ADMIN / FINANCE 接口权限，全程写入不可篡改审计日志（含操作人 IP）

**技术栈**：SpringBoot · Oracle · MyBatis-Plus · Redis · COLA StateMachine · JWT · Spring Retry · ShedLock · CyberSource Flex Microform v2

---

## 秒杀优惠券系统（电讯行业高并发抢券）

**项目描述**：公司运营活动每周定时开放限量优惠券秒杀（如每周五 12:00 限量 500 张），支持积分兑换、会员价抢购多种入口，高峰期瞬时并发量达 5~10 万 QPS。原系统采用同步扣库存 + 单机限流，线上出现超卖投诉，消息队列积压后无自动恢复机制，兑换码系统亦存在刷码漏洞，需全面升级。

**主要工作**：
1. 主导防超卖方案设计，使用 Redis + Lua 原子脚本将库存扣减与用户限购校验合并为单次原子操作，消除并发竞态；配合 Guava 单机限流 + Redis 分布式限流双层削峰，将数据库写压力降低 70%，支撑 5~10 万 QPS 并发场景

2. 将同步下单升级为 Kafka 异步处理架构，引入指数退避重试队列（1 / 5 / 30 分钟三档自动重试）+ 死信队列（DLT）兜底持久化失败订单，临时故障恢复后自动补单，保障用户抢券权益的最终一致性

3. 设计 HMAC 签名兑换码生成方案，并构建 6 层防刷体系（requestId 幂等防网络重试 / CAS 原子核销防并发重入 / 失败 5 次锁定 / 解锁次数超限强制人工审核 / 批次兑换上限 / 过期码定时清理），有效防止社工攻击与批量刷码

4. 基于 K8s 部署并接入 KEDA，配置按 Kafka 消息积压量自动扩缩容消费者副本，活动高峰期积压恢复速度提升 5.5 倍；同时集成 Druid 连接池监控 + Prometheus / Grafana 可视化大盘，实时告警慢 SQL 与连接泄漏

**技术栈**：SpringBoot · Oracle · MyBatis-Plus · Redis · Kafka · K8s · KEDA · Druid · Prometheus · JWT



