# 简历项目经历

---

## 多渠道支付网关系统（电讯行业 · 渣打银行 / CyberSource / Alipay / CCB 四渠道）

**项目描述**：公司原有支付系统强耦合单一渠道，扩展新渠道需大量侵入式改造；同时生产环境频繁出现"客户已扣款、系统显示失败"投诉——银行回调因网络超时丢失后系统无任何兜底机制，财务与客户双方陷入死锁僵局，既无法退款也无发货凭证。系统亦缺乏渠道故障自愈能力，月结账单日若渠道宕机整条支付链路即告中断，需从架构层面全面重建。

**主要工作**：

1. **多渠道策略路由 + PCI DSS 合规**：以策略模式将 SCB（渣打）/ CyberSource（Visa/MC/Amex）/ Alipay / CCB（建行）四条渠道抽象为独立 `PaymentGateway` 实现，统一由 `PaymentGatewayRouter` 按支付方式与 channelOverride 分发；接入 CyberSource Flex Microform v2，卡号在前端经 CyberSource JS SDK 加密为 transientToken，商户后端从不接触明文卡号，彻底规避 PCI DSS 审计范围

2. **引入 COLA StateMachine 管理支付全生命周期**：选型阿里巴巴开源 COLA 状态机（核心 ~400 行，淘宝 / 支付宝 / 钉钉亿级场景验证）；定义 10 个状态 · 13 条合法转换（`INIT→PENDING→SUCCESS/FAILED/TIMEOUT→RECONCILING→SUCCESS|FAILED`；`SUCCESS→REFUNDING→REFUNDED/PARTIALLY_REFUNDED`），无状态设计无需持久化机器实例，并发千笔共用单一 Bean；通过 `guard`（守卫条件 `ctx.isSignatureValid()`）拦截伪造回调，通过白名单拒绝非法跳转（`TIMEOUT→SUCCESS` 必须经 `RECONCILING` 审计中间态）；封装 `PaymentStateMachineService.transition(from, to, ctx)` 统一入口，`@Transactional(REQUIRES_NEW)` + MyBatis-Plus `@Version` 乐观锁保证"校验通过→写DB"原子不可竞争，彻底消除回调并发多线程竞争导致的状态错乱

3. **专项治理"客户付款 · 系统死账"场景**：设计三重幂等兜底（幂等 Token 防重复扣款 + Redis `SET NX` 回调去重 + 定时对账 Job 主动拉取银行未决状态）；TIMEOUT 交易强制经 `TIMEOUT→RECONCILING→SUCCESS/FAILED` 两步修正，留下历史审计踪迹；新增 `ORDER_NOTIFIED` 字段建立双防线通知链路（回调即时通知 + `OrderSuccessNotificationJob` 每 5 分钟扫描 `ORDER_NOTIFIED=0` 兜底补发），确保支付成功后订单系统 100% 感知，死账场景降为零

4. **三态熔断器 + 退款审批工作流**：设计 `GatewayHealthMonitor`（CLOSED / OPEN / HALF_OPEN），ConcurrentHashMap CAS 保证半开期仅单一线程拿到探测机会，连续失败满阈值即告警切断故障渠道，保障月结账单日高可用，主链路 TPS 从 < 500 提升至 > 5000；同步实现多级退款审批工作流（提交→财务一审→经理二审→异步执行），支持多次部分退款与累计上限校验、超时自动驳回，结合 JWT 区分 USER / ADMIN / FINANCE 权限，全程写入不可篡改审计日志（含操作人 IP）

**技术栈**：SpringBoot · Oracle · MyBatis-Plus · Redis · COLA StateMachine · JWT · ShedLock · Spring Retry · CyberSource Flex Microform v2

---

## 秒杀优惠券系统（电讯行业 · 高并发抢券 · K8s 云原生部署）

**项目描述**：公司运营活动每周定时开放限量优惠券秒杀（如每周五 12:00 限量 500 张），支持积分兑换、会员价抢购多种入口，高峰期瞬时并发量达 5~10 万 QPS。原系统同步扣库存 + 单机限流，线上出现超卖投诉；Kafka 消息积压后无自动恢复机制；兑换码存在刷码漏洞，需全面升级。整个系统部署于 K8s 集群，Kafka 以 StatefulSet 方式内置部署于同一集群（非云托管 MQ），通过 K8s Service `kafka-service:9092` 接入，ConfigMap 统一管理集群内连接配置。

**主要工作**：

1. **防超卖 + 动态限流**：Redis + Lua 原子脚本将"检查库存 · 检查用户限购次数 · 扣减库存 · 记录用户"四步合并为单次原子操作，消除并发竞态；设计 `DynamicRateLimiterManager`，根据剩余库存量自动调整令牌桶速率（`库存 × 1.5`），活动冷启动阶段宽放、临近售罄时收紧，配合 Guava 单机 + Redis 分布式双层削峰，将数据库写压力降低 70%，支撑 5~10 万 QPS

2. **K8s 内置 Kafka 异步处理 + 分布式事务最终一致性**：秒杀成功后毫秒级即时返回"抢购成功"并将订单发入集群内 Kafka（StatefulSet 自托管），消费者异步完成积分扣减 + 券记录落库；引入本地消息表（`T_GRANT_TASK`）保证 CAS 原子扣库存与写任务记录同一事务；重试 Job 指数退避补偿（1 / 5 / 30 分钟三档）处理临时故障，超限后路由至死信 Topic（`seckill-order-DLT`）持久化失败订单并触发人工告警，保障用户抢券权益的最终一致性

3. **兑换码 HMAC 签名 + 六层防刷体系**：HMAC-SHA256 签名校验作为第一道门（使用 32 字符安全字符集，去除 `0/O/1/I/L` 易混字符），后接 IP/用户 Redis 限速 · Redisson 分布式锁防并发重入 · DB CAS 原子更新（`WHERE STATUS=0`）防重复兑换 · 失败 5 次锁定（精细化：仅 CAS 并发失败才计次，终态码不触发） · `UNLOCK_COUNT≥2` 强制人工审核防社工攻击六层；`requestId` Redis 幂等缓存（5 分钟 TTL）解决"服务端成功但响应丢失"重试导致用户看到"已被使用"的困惑问题

4. **K8s + KEDA 弹性伸缩 + 全链路监控**：基于 KEDA `ScaledObject` 按 Kafka Lag 自动扩缩容（最小 1 个 / 最大 20 个 Pod），每 15 秒轮询，积压超 1000 条/Pod 时 100% 比例翻倍扩容（15 秒内生效），冷却 5 分钟后缩容，高峰期积压恢复速度提升 5.5 倍；K8s Deployment 配置 `readinessProbe` / `livenessProbe` + `preStop sleep 15s` 实现零停机滚动发布；Druid 替换 HikariCP 开启慢 SQL 告警 + 连接泄漏检测，集成 Prometheus / Grafana 实时监控 QPS、Kafka 积压量与慢查询

**技术栈**：SpringBoot · Oracle · MyBatis-Plus · Redis · Kafka（K8s StatefulSet 自托管）· K8s · KEDA · Druid · Prometheus · Grafana · JWT
