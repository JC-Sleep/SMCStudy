<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/py32_web3_demo/Src/main.c">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/py32_web3_demo/Src/main.c" />
              <option name="originalContent" value="#include &quot;main.h&quot;&#10;#include &quot;dht11.h&quot;&#10;#include &quot;uart.h&quot;&#10;#include &quot;wifi.h&quot;    /* 涂鸦官方 SDK 入口（包含 mcu_api.h / protocol.h） */&#10;&#10;static void SystemClock_Config(void);&#10;&#10;/* 最近一次传感器读数：由主循环写入，all_data_update() 读取（在 protocol.c 中 extern 声明） */&#10;volatile uint8_t g_last_temp = 0U;&#10;volatile uint8_t g_last_humi = 0U;&#10;&#10;int main(void)&#10;{&#10;    /* 1. 初始化 HAL（配置 SysTick 为 1ms 时基） */&#10;    HAL_Init();&#10;&#10;    /* 2. 配置系统时钟为内部 HSI 8MHz */&#10;    SystemClock_Config();&#10;&#10;    /* 3. 外设初始化 */&#10;    UART1_Init();           /* USART1 → WBR3，9600bps，同时使能 RXNE 中断 */&#10;    DHT11_Init();           /* PA0 输出模式，空闲拉高 */&#10;    wifi_protocol_init();   /* 涂鸦 SDK 初始化：清空 RX 缓冲区、复位状态机 */&#10;&#10;    uint32_t lastReportTick = 0U;&#10;&#10;    while (1)&#10;    {&#10;        /* 驱动涂鸦串口协议状态机（解析来自 WBR3 的心跳、查询等帧） */&#10;        wifi_uart_service();&#10;&#10;        /* 每 30 秒读取一次 DHT11 并上报到涂鸦云 */&#10;        if ((HAL_GetTick() - lastReportTick) &gt;= 30000UL)&#10;        {&#10;            lastReportTick = HAL_GetTick();&#10;&#10;            DHT11_Data_t sensor;&#10;            if (DHT11_Read(&amp;sensor) == DHT11_OK)&#10;            {&#10;                /* 保存到全局变量供 all_data_update() 使用 */&#10;                g_last_temp = sensor.temperature;&#10;                g_last_humi = sensor.humidity;&#10;&#10;                /* 通过涂鸦官方 SDK 上报 DP 到云端 */&#10;                mcu_dp_value_update(DPID_TEMP_CURRENT,   (u32)sensor.temperature);&#10;                mcu_dp_value_update(DPID_HUMIDITY_VALUE, (u32)sensor.humidity);&#10;            }&#10;            /* 校验失败时静默跳过，下一个 30s 周期重试 */&#10;        }&#10;    }&#10;}&#10;&#10;/*&#10; * SystemClock_Config&#10; * 使用内部 HSI 8MHz，HCLK = PCLK = 8MHz，Flash 等待 0 周期。&#10; */&#10;static void SystemClock_Config(void)&#10;{&#10;    RCC_OscInitTypeDef osc = {0};&#10;    RCC_ClkInitTypeDef clk = {0};&#10;&#10;    osc.OscillatorType      = RCC_OSCILLATORTYPE_HSI;&#10;    osc.HSIState            = RCC_HSI_ON;&#10;    osc.HSIDiv              = RCC_HSI_DIV1;&#10;    osc.HSICalibrationValue = RCC_HSICALIBRATION_8MHz;&#10;    if (HAL_RCC_OscConfig(&amp;osc) != HAL_OK)&#10;    {&#10;        APP_ErrorHandler();&#10;    }&#10;&#10;    clk.ClockType      = RCC_CLOCKTYPE_HCLK | RCC_CLOCKTYPE_SYSCLK | RCC_CLOCKTYPE_PCLK1;&#10;    clk.SYSCLKSource   = RCC_SYSCLKSOURCE_HSI;&#10;    clk.AHBCLKDivider  = RCC_SYSCLK_DIV1;&#10;    clk.APB1CLKDivider = RCC_HCLK_DIV1;&#10;    if (HAL_RCC_ClockConfig(&amp;clk, FLASH_LATENCY_0) != HAL_OK)&#10;    {&#10;        APP_ErrorHandler();&#10;    }&#10;}&#10;&#10;/*&#10; * APP_ErrorHandler&#10; * 不可恢复的错误：关中断后死循环。&#10; * 调试时可在此处打断点观察调用栈。&#10; */&#10;void APP_ErrorHandler(void)&#10;{&#10;    __disable_irq();&#10;    while (1) {}&#10;}&#10;&#10;#ifdef USE_FULL_ASSERT&#10;void assert_failed(uint8_t *file, uint32_t line)&#10;{&#10;    (void)file;&#10;    (void)line;&#10;    APP_ErrorHandler();&#10;}&#10;#endif&#10;" />
              <option name="updatedContent" value="#include &quot;main.h&quot;&#13;&#10;#include &quot;dht11.h&quot;&#13;&#10;#include &quot;uart.h&quot;&#13;&#10;#include &quot;wifi.h&quot;    /* 涂鸦官方 SDK 入口（包含 mcu_api.h / protocol.h） */&#13;&#10;&#13;&#10;static void SystemClock_Config(void);&#13;&#10;&#13;&#10;/* 最近一次传感器读数：由主循环写入，all_data_update() 读取（在 protocol.c 中 extern 声明） */&#13;&#10;volatile uint8_t g_last_temp = 0U;&#13;&#10;volatile uint8_t g_last_humi = 0U;&#13;&#10;&#13;&#10;int main(void)&#13;&#10;{&#13;&#10;    /* 1. 初始化 HAL（配置 SysTick 为 1ms 时基） */&#13;&#10;    HAL_Init();&#13;&#10;&#13;&#10;    /* 2. 配置系统时钟为内部 HSI 8MHz */&#13;&#10;    SystemClock_Config();&#13;&#10;&#13;&#10;    /* 3. 外设初始化 */&#13;&#10;    UART1_Init();           /* USART1 → WBR3，9600bps，同时使能 RXNE 中断 */&#13;&#10;    DHT11_Init();           /* PA0 输出模式，空闲拉高 */&#13;&#10;    wifi_protocol_init();   /* 涂鸦 SDK 初始化：清空 RX 缓冲区、复位状态机 */&#13;&#10;&#13;&#10;    uint32_t lastReportTick = 0U;&#13;&#10;&#13;&#10;    while (1)&#13;&#10;    {&#13;&#10;        /* 驱动涂鸦串口协议状态机（解析来自 WBR3 的心跳、查询等帧） */&#13;&#10;        wifi_uart_service();&#13;&#10;&#13;&#10;        /* 每 30 秒读取一次 DHT11 并上报到涂鸦云 */&#13;&#10;        if ((HAL_GetTick() - lastReportTick) &gt;= 30000UL)&#13;&#10;        {&#13;&#10;            lastReportTick = HAL_GetTick();&#13;&#10;&#13;&#10;            DHT11_Data_t sensor;&#13;&#10;            if (DHT11_Read(&amp;sensor) == DHT11_OK)&#13;&#10;            {&#13;&#10;                /* 保存到全局变量供 all_data_update() 使用 */&#13;&#10;                g_last_temp = sensor.temperature;&#13;&#10;                g_last_humi = sensor.humidity;&#13;&#10;&#13;&#10;                /* 通过涂鸦官方 SDK 上报 DP 到云端 */&#13;&#10;                mcu_dp_value_update(DPID_TEMP_CURRENT,   (u32)sensor.temperature);&#13;&#10;                mcu_dp_value_update(DPID_HUMIDITY_VALUE, (u32)sensor.humidity);&#13;&#10;            }&#13;&#10;            /* 校验失败时静默跳过，下一个 30s 周期重试 */&#13;&#10;        }&#13;&#10;    }&#13;&#10;}&#13;&#10;&#13;&#10;/*&#13;&#10; * SystemClock_Config&#13;&#10; * 使用内部 HSI 8MHz，HCLK = PCLK = 8MHz，Flash 等待 0 周期。&#13;&#10; */&#13;&#10;static void SystemClock_Config(void)&#13;&#10;{&#13;&#10;    RCC_OscInitTypeDef osc = {0};&#13;&#10;    RCC_ClkInitTypeDef clk = {0};&#13;&#10;&#13;&#10;    osc.OscillatorType      = RCC_OSCILLATORTYPE_HSI;&#13;&#10;    osc.HSIState            = RCC_HSI_ON;&#13;&#10;    /* HSIDiv 字段仅在 PY32F0xx SDK V2.x+ 存在，V1.5.0 无此字段，删除 */&#13;&#10;    osc.HSICalibrationValue = RCC_HSICALIBRATION_8MHz;&#13;&#10;    if (HAL_RCC_OscConfig(&amp;osc) != HAL_OK)&#13;&#10;    {&#13;&#10;        APP_ErrorHandler();&#13;&#10;    }&#13;&#10;&#13;&#10;    clk.ClockType      = RCC_CLOCKTYPE_HCLK | RCC_CLOCKTYPE_SYSCLK | RCC_CLOCKTYPE_PCLK1;&#13;&#10;    clk.SYSCLKSource   = RCC_SYSCLKSOURCE_HSI;&#13;&#10;    clk.AHBCLKDivider  = RCC_SYSCLK_DIV1;&#13;&#10;    clk.APB1CLKDivider = RCC_HCLK_DIV1;&#13;&#10;    if (HAL_RCC_ClockConfig(&amp;clk, FLASH_LATENCY_0) != HAL_OK)&#13;&#10;    {&#13;&#10;        APP_ErrorHandler();&#13;&#10;    }&#13;&#10;}&#13;&#10;&#13;&#10;/*&#13;&#10; * APP_ErrorHandler&#13;&#10; * 不可恢复的错误：关中断后死循环。&#13;&#10; * 调试时可在此处打断点观察调用栈。&#13;&#10; */&#13;&#10;void APP_ErrorHandler(void)&#13;&#10;{&#13;&#10;    __disable_irq();&#13;&#10;    while (1) {}&#13;&#10;}&#13;&#10;&#13;&#10;#ifdef USE_FULL_ASSERT&#13;&#10;void assert_failed(uint8_t *file, uint32_t line)&#13;&#10;{&#13;&#10;    (void)file;&#13;&#10;    (void)line;&#13;&#10;    APP_ErrorHandler();&#13;&#10;}&#13;&#10;#endif&#13;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>