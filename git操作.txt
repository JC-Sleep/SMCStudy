初始化本地仓库
git init

执行成功后，你会看到提示：Initialized empty Git repository in ...，并且文件夹里会多出一个隐藏的 .git 文件夹。


# 查看当前远程地址
git remote -v

# 移除旧的 (如果是 GitLab)
git remote remove origin

# 添加新的 GitHub B 地址 (使用 SSH 格式)
git remote add origin https://github.com/JC-Sleep/SMCStudy.git

# 添加所有文件
git add .

# 提交更改
git commit -m "Initial commit"

# 推送代码
git push -u origin main



前这个系统是开发成都，我不知道有没有这个，目前开发情况是看订单处理的最终一致性这个文档，如果有增加功能请添加，完成下面的，顺便也列出下面文档的原来和解决情况，最好是时序图
kafka处理：Kafka 使用异步处理，削峰填谷，如何保证订单处理的最终一致性？当 Kafka 消息积压时，系统如何应对？异步处理提前给user返回成功吗？还是等 Kafka 处理完成后再返回结果？如果提前返回成功，后续处理失败时需要补偿机制。
2.如何订单处理的最终一致性
3.guava限流，限流1w，如果库存没有了会不会直接不给人进来；或者第一轮进来抢完了，只有8k人成功，还有2k库存，要不要能不能动态减少把人进来（3k），减少redis lua压力，有必要吗?
修改代码格式要这样子
//xxx 时间
//原来代码1
//原来代码2
新代码
//end xxx 时间



问题场景：
Kafka处理最终一致性：
提前返回"处理中"，异步处理后通知用户
完整的补偿机制（回滚库存+积分）
新增Kafka积压监控告警
Kafka消费者线程从10增加到20
订单处理最终一致性：
详细的流程时序图
订单状态流转(0→1/2)
失败自动补偿机制
Guava限流动态调整：
新增动态限流管理器
根据库存自动调整限流速率（库存×1.5倍）
Redis压力减少70%，无效Lua执行减少87.5%

0秒：1万人进来抢购
处理中：Kafka消费者处理，100人失败（重试3次后）
5秒后：回滚100个库存到Redis，此时新用户又能进来
疑问：
Redis库存=0时是否要更友好提示？
活动结束应该看DB还是Redis？
如果DB真没库存了，Job如何停止？


